# 工程清债核销清单（全量打包批·批次5，2026-09-20）

> 对照 `docs/Sprint4-全库审查修复计划-2026-09-19.md`（W-A2 剩余范围）与
> `docs/Sprint4完工审查报告-2026-09-19.md`（遗留行动项 1~3 + P2×21）逐条核销。
> 原则：勾销必须附证据（commit/行号/测试名）；无法核销的写明原因留档，不粉饰。

## 一、W-A2 补清算链（修复计划 L50-54 剩余范围）

| 条目 | 处置 | 证据 |
|---|---|---|
| W-A2① 规则4 豁免加 kill_liquidation | ✅ **核销**（先前施工已落地，本轮核实） | `risk/engine.py` rule_trading_session（L210-222，sell 限定）；正负用例 `tests/test_risk_engine.py::test_rule4_kill_liquidation_exempt_non_session`（kill 单豁免过 / 普通卖单照拒） |
| W-A2② `_decision_from_row` 恢复 kill_liquidation | ✅ **已关闭**（C-ARC 补修 commit 8828e98，2026-09-19 核验；非本批范围） | 修复计划 L52 划线记录 + `execution/runner.py:320-327`（仅 sell）+ `tests/test_execution.py:1226+` |
| W-A2③ 规则18 流动性豁免 + 规则3 report_only 降级留痕放行 | ✅ **核销**（先前施工已落地，本轮核实） | `risk/engine.py` rule_health L185-201（kill_liquidation_health_exempt 事件留痕）、rule_liquidity L643-666（1% 参与率豁免）；正负用例 `tests/test_risk_engine.py::test_rule3_health_kill_liquidation_exempt`（kill 放行+普通单 report_only）、`::test_rule18_kill_liquidation_exempt`（150 万>5 万豁免过） |

结论：W-A2 三条全部关闭，链路测试（09:00 propose → 过规则4/3/18 → pending → 停机期 confirm 过规则5 → 成交）见 `tests/test_execution.py` kill 链用例组。

## 二、Sprint4 完工审查遗留行动项

| # | 条目 | 处置 | 证据/原因 |
|---|---|---|---|
| 1 | 周一（09-21）确认 crontab 首 tick 真实执行 + 09-21/22 DoD③ 两日生产验证 | ⏳ **时间未到，留批次 7**（2026-09-25 盘后条件触发）收录。本批只读核实：crontab 两条在位（`*/30 8-20` catchup + `*/5 9-15` recorder，.venv 解释器、日志归 logs/catchup.log）；周日无 tick 可验证 | crontab -l 输出（2026-09-20 只读核验，未改动） |
| 2 | em 日线源连宕，周一 bar 供给依赖 tx 兜底链 | ✅ **状态更新核销**（运行状态项，自愈环已验证）：批次 0 实测 em K线域（push2his）当日全程不可达、datacenter（fhps）正常——非断网，为源级封禁/故障；tx qfq 兜底 96.5% 覆盖在位；PENDING+exit2 自愈环 W-D 验证过。周一若 em 仍宕按既有兜底链运行 | 批次0 报告 §Gate 0-A（docs/批次0-qfq数据层报告-2026-09-20.md） |
| 3 | P2 清单 21 项排期（优先 ⑦⑧⑨⑬） | ✅ **核销**：本批批次 4 全量处置完毕（4a/4b/4c 三域三 commit，含优先项 ⑦⑧⑨⑬ 均为行为修复） | commit `d243a68`（4b 数据域）/ `37992c7`（4a 风控执行域）/ `6f4ca85`（4c LLM 上下文与选型域）；逐项见下表 |

## 三、P2×21 逐项核销表（Sprint4 完工审查报告 L26-30）

| 项 | 内容（摘） | 处置 | 证据 |
|---|---|---|---|
| ① | dd_base 分支污染历史回放 | ✅ 修复 | `execution/runner.py` effective_peak 回放守卫（before<dd_base_date 回退 250 行窗口）；用例 `test_mark_to_market_replay_before_dd_base_not_zeroed` |
| ② | 应急显式设计价 = T 日跌停价的 T+1 幻影价 | ✅ 修复（语义变化，ADR 附记 E） | `runner.py` confirm 应急卖单 min(设计价, 执行日跌停价)；kill_liquidation 维持原口径 |
| ③ | 周五应急单 run_date=周六，周一 failsafe 查不到 | ✅ 修复（语义变化，ADR 附记 E） | `signals/limit_halt.py::_next_exec_date` 周末顺延；用例 `test_next_exec_date_skips_weekend`、`test_postclose_scan_friday_run_date_is_monday` |
| ④ | _explicit_price 死标记+挂起单无主动通知 | ✅ 修复 | 死标记删除（从未被消费）；挂起/拒执单 notify 三处；用例在 `tests/test_execution.py` |
| ⑤ | lifetime_peak/kill_count 无消费方 | ✅ 修复（观测接线） | `review/daily.py` 日报熔断史累计行；用例 `test_daily_report_kill_history_observation_line` |
| ⑥ | dd_base 分支无 250 行窗口 | ✅ 修复 | effective_peak dd_base 分支补 window；与 ① 同用例覆盖 |
| ⑦ | recalc_tx_pct "前一行"过滤集口径 | ✅ 修复（语义变化，ADR 附记 F） | 改上一日历行（与 audit tx_pct_divergent 对齐）；用例 `tests/test_qfq_rebuild.py::TestP2Batch4b` |
| ⑧ | 重刷失败坏窗照样 commit | ✅ 修复 | SAVEPOINT 弃窗（与漂移路径对称）；同上用例 |
| ⑨ | 不变量闸对高 d/价票不可收敛 | ✅ 修复（语义变化，ADR 附记 F） | fetch_log 冷却窗 REBRUSH_COOLDOWN_DAYS=7；同上用例 |
| ⑩ | tx pct 存量 141 行/9 票（audit --fix 即收敛） | ✅ **核销（前提证伪，无需执行）**——用户授权执行后经副本实测证伪：生产库字节副本上 `check_db` 判 **tx_pct_divergent=0**（total=40 全为 pct_out_of_range 已知存量）；`recalc_tx_pct` 返回 fixed=141 但**实际值变化 0 行/0 票**——141 是"无条件重写行"计数器（ex-div 窗口内按 qfq 环比重写，写前写后值相同），完工审查误读为"错误行数"。数据本已正确，执行 --fix 为零收益写放大，**未在生产库执行** | 副本实测 2026-09-20（/tmp/market_p10check.db，生产库只读未动）；谓词=check_db W-B3 原判定；audit.log 无 fix_tx_pct 执行记录、market.db 自 09-19 05:56 冻结 |
| ⑪ | legu 兜底不校验统计日期 | ✅ 修复（fail-closed） | `data/breadth.py` 统计日期匹配（em+legu 组合档不采用、legu 档整档判失败走兜底链）；用例 `tests/test_breadth.py` + `test_sprint4_b` 契约更新 |
| ⑫ | `profile IS NULL` 潜伏混算闸 | ✅ 修复（附负向用例，预注册要求） | `review/signal_eval.py::_signal_frame` + `signals/signals.py::_write_factor_crowding` 两处删除；负向用例 `test_p2g12_signal_frame_excludes_null_profile_rows` + `tests/test_signals.py` |
| ⑬ | 有痕降级日无自动愈合 | ✅ 修复（语义变化，ADR 附记 D） | `pipeline/catchup.py::_mm_done` 愈合分支（滞后代码任一该日已有 bar → 未完成）；防循环边界保持；用例 `tests/test_review.py` |
| ⑭ | realized_pnl 与持仓成本口径差 | ✅ 核实留档（计算不变） | `ai/bundle.py` 注释：移动成本含费用 vs position.cost 不含费用（每股差买入佣金分摊）；note 文案已注明 |
| ⑮ | orphan 卖出/混源跨缺口无测试用例 | ✅ 补用例 | `test_p2c15_realized_pnl_orphan_sell`、`test_p2c15_realized_pnl_mixed_lots_across_gaps` |
| ⑯ | momentum "同源"措辞过强 | ✅ 措辞降级（不改数值） | `signals/backtest.py` docstring + metadata：同权重同公式，停牌票可分叉（≤3/816，不翻盘 FAIL） |
| ⑰ | pass 门信任 universe 标签 | ✅ 防御性校验 | `_pool_is_core_watchlist`（码集与 watchlist_core 完全相等才判 core；config 不可读不拦）；用例在 `tests/test_sprint4_d.py` |
| ⑱ | test_wd7_pending_cleared 同义反复 | ✅ 修复 | 测试改经 `_clear_pending_today()` 直调（不再自己 unlink stub） |
| ⑲ | postclose 清今日 PENDING 与 --date 组合误删 | ✅ 修复（语义收窄，ADR 附记 D） | `_clear_pending_today(trade_date)`：仅本次成功跑的就是今日才清 |
| ⑳ | "盯市成功+日报失败"当日不自愈 | ✅ 核实留档（W-D2 刻意收窄，行为不变） | `pipeline/catchup.py` 注记 + ADR 附记 D：次日 catchup 步骤 1 按缺失补跑 |
| ㉑ | deploy/ 两份 plist 模板未删 | ✅ 删除 | commit `37992c7` 删 `deploy/com.agsickle.catchup.plist`、`deploy/com.agsickle.recorder.plist`（W-D1 调度收敛 crontab+ZCode automation）；catchup docstring 失效描述同步（`6f4ca85`） |

## 四、无法/暂不核销条目汇总（不粉饰）

1. **P2⑩（已闭环，2026-09-20 深夜补充）**：用户授权执行后，生产库字节副本实测证伪前提（详见上表⑩行）——141 行为计数器误读、数据本已正确，未执行写库。授权作废无需使用。
2. **行动项 1**：crontab 首 tick 与 DoD③ 两日生产验证——时间窗 09-21/22，本批（09-20）无法执行，批次 7（09-25 盘后触发，已设一次性自动化）收录。
3. **批次 0 遗留（跨批关联，非本清单范围但须披露）**：Gate 0-A 的 159 票 em 重拉被 em 源不可达阻断（em 恢复后 `gate0a-repair` 补完无需新预注册）；Gate 0-B/0-D 判据与 tx 加法型复权现实的结构性冲突——用户已裁决 **a+c 组合**，新预注册口径草案待确认（docs/qfq等比口径施工方案-2026-09-20.md）。

## 五、验证

- 批次 5 核验方式：全只读（代码行号核对 + 既有测试读取 + crontab -l），零写库、零行为变更。
- 本批全量测试：`tests/run_all.py` 31/31 文件通过（2026-09-20，批次 6 合入后复验）。
