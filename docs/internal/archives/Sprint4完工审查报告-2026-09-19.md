# Sprint4 完工多 agent 审查报告（2026-09-19）

审查对象：Sprint4 全部施工（94c7ace → 102de4d 六提交 + W-D1 调度重建）。方式：4 个并行对抗审查 agent（批次A 风控执行 / 批次B+C 数据与LLM上下文 / 批次D+选型复现 / 跨批次集成与运行面），全部只读、自带沙箱复跑 run_all 与 e2e、生产库只读 SQL 取证；主审对全部 P1 逐条代码复核后才修复。

## 总体判定

**四个域全部 pass-with-fixes，无 P0。** 3 项 P1 当日修复并合入（a2af20f），P2 留档排期。

| 域 | 判定 | 要点 |
|---|---|---|
| 批次A 风控执行 | pass-with-fixes | kill 幂等/dd_base 生命周期/规则21 三处/应急跨日主链扎实；P1×2（豁免伪造、跌停基准错位） |
| 批次B+C 数据与LLM | pass-with-fixes | IC 过滤/qfq 单源/除权重算/bundle 改造主体成立；P2×9（无 P0/P1 残留） |
| 批次D+选型复现 | pass-with-fixes | **W-D6 独立复现成功**：momentum 年化 +17.80%（存档 +17.98%，差 1 个交易日）、MDD −47.78% 逐位一致、2024/2025 逐年收益逐位一致——FAIL 判定可复现 |
| 跨批次集成与运行面 | pass-with-fixes | DoD ①②④⑤ 全过（③未到期）；P1×1（catchup 目录错位，周一必炸） |

## P1×3（已修复，a2af20f）

1. **catchup 步骤2a/2b bundle 目录错位**（集成域发现）：bundle/midday 落盘在**当日**会话目录（premarket/midday run_date=今天），检查却指向 `latest_td`（盘中=daily_bar 最新=上一交易日）→ 恒判"缺失"；叠加新 crontab `*/30`，周一 08:30–15:30 每 tick 都会整套重跑 premarket。修复：目录改当日会话目录；bundle_missing 腿经修正目录后天然幂等（早晨生成过→mtime=今天→不再补），保持"任何时候补"语义；另加 premarket 单实例 flock（防 crontab 与 09:00 调度节点并发整套重跑）。
2. **kill_liquidation/emergency_scan 提权键输入侧未剥离**（批次A 域发现）：两键已获服务端特权（豁免规则3/4/18、跨日/TTL 闸、stale 设计价执行），`load_decision_file`（propose --file 外部输入）不剥离 → 文件注入即提权，且经 input_snapshot 被 confirm 路径恢复。修复：与 skip_gate 同款在 load_decision_file 剥离（decide.py 白名单构造本就不含两键，LLM 主路径已安全；合法注入仅服务端 limit_halt/resolve_liquidations）。
3. **决策路径涨跌停基准错位一天**（批次A 域发现）：`paper.prev_close` 用 `offset=1`（次新 bar）——盘中 daily_bar 最新=T-1，取到 T-2；连续下跌时 [真跌停, 旧跌停] 区间的合法止损卖单被规则14/paper 双双误拒（恰是 kill 高发场景）。修复：`repo.latest_close` 增 `before=执行日` 口径（严格早于执行日的最新收盘——盘中取 T-1、盘后仍 T-1、次日自动滚动到 T），执行日由调用链显式传递（prev_close/portfolio/_pre_trade_guards/扫描器；生产=墙钟今天，回放/测试显式传）。修法演进记录：offset=0 方案被测试否决（盘后场景会错取当日 bar），`before=执行日` 才是三场景全对的唯一口径。

修复验证：23/23 测试全绿（新增 3 个回归用例：盘中 latest_td=昨天不误判缺失、剥离提权键、盘中 prev_close）+ e2e 3/3 + 生产 logs 零污染。

## P2 留档清单（未修，按域归档待排期）

**风控执行**：①dd_base 分支污染历史回放（dd_base_date 晚于补跑日报日 → 回撤记 0 且覆写 history drawdown 列）；②应急"显式设计价"= T 日跌停价，T+1 连续死封时 paper 按高 ~11% 幻影价成交（开板情形反向保守）；③周五盘后生成的应急单 run_date=周六，周一 failsafe 查不到（自愈=stuck 计数+周一重扫，但连续跌停退出晚 3 天）；④`_explicit_price` 死标记+挂起单无主动通知；⑤lifetime_peak/kill_count 无消费方（ADR-S4-1 的对冲观测未接线）；⑥dd_base 分支无 250 行窗口。
**数据**：⑦recalc_tx_pct 的"前一行"取过滤集内上一条而非上一日历行（混源缺口含除权会写多日收益；当前 7 对混源相邻未触发）；⑧增量窗不变量命中且重刷失败时坏窗照样 commit（与漂移路径"放弃写入"不对称）；⑨加法型口径下不变量闸对高 d/价票不可收敛（除权+大跌日或触发反复全史重刷——建议 fetch_log 冷却窗）；⑩tx pct 存量余 141 行/9 票（非 watchlist 票无再修路径，audit --fix 即收敛）；⑪legu 兜底不校验统计日期（盘前可能把昨日活跃度写成今日行）；⑫`profile IS NULL` 兼容条款是潜伏混算闸（当前 NULL 行=0，建议删除）。
**LLM 上下文**：⑬有痕降级日无自动愈合（--date 降级盯市+PENDING 清除后，当晚数据到位也不重跑——建议 _mm_done 加"价格滞后且该日已有 bar→未完成"分支）；⑭realized_pnl 与 paper 持仓成本口径差每股佣金（note 已注明）；⑮orphan 卖出/混源跨缺口无测试用例。
**选型**：⑯momentum"同源"对停牌票不成立（面板 ma/mom 按日历位置 rolling vs 生产按 bar 序列，影响 ≤3/816 票、不翻盘 FAIL 结论，metadata 措辞应降级）；⑰pass 门信任 universe 标签不校验池（无现实触发路径）；⑱test_wd7_pending_cleared 同义反复（测试自己 unlink）；⑲postclose 清"今天"PENDING 与 --date 补历史组合会误删今日 PENDING（仅人工提醒物）；⑳"盯市成功+日报失败"当日不自愈（次日 catchup 补，W-D2 刻意收窄）。
**集成**：㉑deploy/ 内两份 plist 模板未删（复活隐患注记）。

## 验证为正确（抽核要点）

- kill 链端到端：decision_id=None 绕唯一索引、(decision_id,code) 二元幂等、kill_executed 血缘事件、dd_base 分段/递延补写幂等、可重入锁跨进程语义保持。
- 规则21：f19-f48 重映射无旧偏移残留消费方、seal 量纲链手→股/亿→元自洽、触板未封死不生成（真死封/未封死双用例）、stuck 首日过滤两场景正确。
- 应急跨日：跨日闸三态+TTL 按 run_date 15:05+dedupe 收紧无死循环；`emergency_direct_exec=false` 下 09:14 failsafe 只通知不成交（代码级证明）。
- W-D6 复现：2024/2025 逐年收益逐位一致（确定性），pass=full 硬门、P2-4 透传后默认路径逐位不变、backfill 三 profile 名实一致。
- 运行面：crontab 两条语法/路径正确、launchd 无 agsickle 残留、09:14 自动化提示词逐断言核验（confirm 命令格式错误已修正为 `confirm --decision-id <id>`）、config.json 六提交未触碰、webapp 接口 200、生产库终态（signal 三 profile/拥挤 active/指数宽度新鲜至 09-18）自洽。
- 文档：ADR-S4/CONSTRAINTS v1.2/决策策略与工作流的每条可验证断言与代码无漂移。

## 遗留行动项

1. 周一（09-21）开盘前：确认 crontab 08:00 首 tick 真实执行（中文路径+TCC 历史坑）；09-21/22 连续两日生产验证（DoD ③）。
2. em 日线源连宕第 4 天，周一 bar 供给依赖 tx 兜底链，若 tx 也故障则 PENDING+exit2 自愈环兜底（已验证）。
3. P2 清单 21 项按上表排期；优先 ⑦⑧⑨（数据写路径，调度恢复后可能首次发作）与 ⑬（降级日愈合）。
