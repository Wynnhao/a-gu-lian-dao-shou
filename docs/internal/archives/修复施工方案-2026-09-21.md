# 修复施工方案（对抗审查回应批）— 2026-09-21

裁决来源：`docs/多agent全库审查报告-2026-09-21.md`（5 路对抗审查，主会话亲验）。本方案把其结论转为可执行批次。**全程生产库零写入**——任何需要动生产库的动作（qfq 重拉、40 行 pct 修复、valuation_mode 加列执行）一律"代码+测试落地、生产执行进 §7 待授权清单"。

## §0 范围矩阵

| 审查编号 | 事项 | 处置 | 批次 |
|---|---|---|---|
| P1-1★ | 规则3 lag 自然日口径 → 09-28/10-08 全链瘫痪+停牌票 poison | **修代码** | 0 |
| P1-2★ | 应急单 run_date 节假日死穴（09-24 盘后即触发场景） | **修代码** | 0 |
| P1-3★ | 周报 weekday()==4 → 中秋周/国庆周漏发 | **修代码** | 0 |
| P0-A | qfq 等比方案 override 缺口+名单抓错病 | **修订方案稿**（待用户批准，不碰代码/库） | 1 |
| P0-B | 判据购物棘轮：退出条件/判据冻结/A-D弃权/附记B反方论点 | 治理落地 | 2 |
| P0-C | 归档账本二元出口 → 三态+Gate0+阈值推导 | 代码+标签更正 | 2 |
| P1-4 | 09:14 failsafe 节点不可达 | 修代码 | 3a |
| P1-5 | 备份不幂等吞体检结论 | 修代码 | 3a |
| P1-6 | 加法型 qfq 季节性复发 | 披露口径（随 P0-A 方案修订） | 1 |
| P1-7 | ex日 pct_chg 语义错+40行残渣 | 修代码（判据对齐），生产数据修复待授权 | 3b |
| P1-8 | 回测宇宙断粮 | verdict 宇宙守卫（cheap 版）；universe 补数另行 | 3b |
| P1-9 | 估值链 fail-open | valuation_mode 列（代码+临时库），生产加列待授权 | 3b |
| P1-10 | full 宇宙成分回溯偏差 | **不做**（研究级，另立预注册），留档 | — |
| P1-11 | qfq 剔除配对复算证据 | 回填批次0 报告 | 3c |
| P1-12 | 预注册不可复核 | 两段式纪律成文 CONSTRAINTS | 2 |
| P1-13 | X1/X2"巧合"措辞 | 报告+策略库 §10.5 改写 | 3c |
| P1-14 | P6 验收 KPI 混记 | 流程/策略 KPI 分列 | 3c |
| P2×20 | 见审查报告 §5 | 分域清债（§4 批次3） | 3 |

明确不做：P1-10、R1/X 外部概念指数复测（另立预注册批次）、P2 快照 order 匹配 decision_id（评估留档）、bundle 终档溢出（既定设计）、review/weekly.py 手补 CLI 不改。

## §1 全局守则（每批次/每 agent 必读）

1. 解释器一律 `./.venv/bin/python3`（裸 python3 缺依赖）。
2. 生产库（data/market.db、data/agent.db）**零写入**：审查/核对用 `file:...?mode=ro` URI；新增列/改数据的代码只跑临时库测试，生产执行进 §7。
3. 测试隔离：新测试不得写真实库、不得写真实 logs/（用 AGSICKLE_* 沙箱 env，直接赋值非 setdefault）；run_all 收尾统一注入 AGSICKLE_LOG_DIR。
4. 每项修复带**正反用例**（触发场景+不触发场景+回退场景），跟随所在测试文件惯例。
5. 收官门：每批次结束主会话跑 `./.venv/bin/python3 tests/run_all.py`，全绿后**单个原子 commit**，文案格式 `批次N(修复批): <摘要——改了什么/为什么/DoD如何满足>`。
6. **已归档研究判据零修改**：P0-C 只增 INSUFFICIENT-DATA 标签与 Gate0 前置，任何阈值/比较符/已归档 FAIL 数字不得变动（diff 审查把关）；策略库 §10 更正仅加"更正说明"，不删原文。
7. 不动 crontab、不动 ZCode automation（本批修复全部在代码层）；confirm 跨日闸、failsafe `run_date==today` 查询语义**保留不动**（只改 run_date/触发条件的生成端）。
8. 拍板边界：profile 切换永不自动；§7 清单内的数值条款给出建议稿并标注"待用户拍板"，不得自行定稿。

## §2 修复判据（DoD，逐项可验收）

**D-0a 交易日历 helper**（data/repo.py 新增）
- `is_trading_date(conn, d)` / `next_trading_date(conn, d)` / `last_trading_date_on_or_before(conn, d)`：查 `trade_calendar(date)` 表；**回退语义**：表空/查无（覆盖期外）时降级为周末口径（next=跳过周六日）并 `log.warning` 一次，行为不得劣于现状。
- 用例：表内命中（跳过 09-25）、表内缺口（10-01~07 一次跳到 10-08）、表空回退三场景。

**D-0b 规则3 健康检查**（risk/blacklist.py health_check）
- lag 腿：`expected = last_trading_date_on_or_before(conn, yesterday)`；仅当 `latest < expected` 报"数据滞后"。验收：模拟 09-28 盘前（latest=09-24）**不报**；模拟"周一缺周一数据"（latest=上周五<expected=周一）**报**；日历缺失回退现行为。
- 缺 bar 腿：仅对"最近 5 个交易日内有过 bar 但缺最新交易日 bar"的票报 issue（长停牌/退市票不再永久 poison）。验收：停牌 10 日票不报、昨日有票今日缺报。
- 副作用核查：`execution/runner.py:389` 消费方签名不变。

**D-0c 应急单 run_date**（signals/limit_halt.py `_next_exec_date`）
- 改为 `next_trading_date(conn, now+1d)`；`run_postclose_scan` 已持有 conn（无则 get_conn），透传。
- 验收：09-24 盘后 → run_date=09-28；09-30 盘后 → 10-08；普通周四 → 周五；日历缺失 → 回退周末口径。跨日闸/failsafe 查询零改动，run_date==today 语义自洽。

**D-0d 周报触发**（pipeline/postclose.py:363、pipeline/catchup.py 补跑分支）
- 触发条件改"td 是本周最后一个交易日"（比较 td 与 next_trading_date 的 ISO 周；next 不存在=数据末端，视作触发）。`--weekly` 手动旗标保留。
- 验收：09-24（周四，周五休市）触发且产物为 W39；09-22 不触发；普通周五触发；日历缺失回退 `weekday()==4` 口径。catchup 补跑分支同条件。

**D-1 qfq 方案修订**（docs/qfq等比口径施工方案-2026-09-20.md，只改文档）
- 新增 S0/P0 前置项：`qfq_source_override` 表+fetcher 拦截行为（名单票在 em 不可达期间：**跳过该票增量 qfq 写入并 notify**，不得落 tx 加法），带独立 gate（G-O1：名单票 source 全量对账）与调度主体（挂 catchup 周期体检或 postclose）。
- 名单口径改写：由"无事件对 |ret_qfq−ret_raw| 失真×窗口贡献"驱动；附只读计算脚本产出排序清单（落 logs/reports/，含审查给出的 19 只高失真 watchlist_core 票必入围）；watchlist_core 51 票全量 em 重拉作为替代方案列出，两案并陈待拍板。
- 披露指标 p50 → 分位表+按票最大值；§1 背景补 P1-6 季节性复发表述（"每年 5-9 月除权后 1-2 个月高 D 票因子带偏，非一次性历史问题"）。
- DoD：修订稿自查"重拉完成后 em 断供一周"推演无静默回退路径；**不执行任何重拉**。

**D-2 治理四件套**
- 2a docs/决策策略与工作流.md §7 增"观察期退出条款（预注册草案）"：建议稿=「W+4 周（2026-10-19）重跑 W-D6 口径仍 FAIL，或生产可实现超额<X 连续 N 日 → 停用 profile、空仓等研究线」——X/N 数值标注待拍板；ADR-Sprint4 附记 B 补反方论点（"空仓直到有 profile pass"被否决的理由）与替代方案。
- 2b CONSTRAINTS.md 升 v1.3：判据冻结条款（pass 判据版本随产物固化；同批结果可见后不得改判据；改动须新 ADR）+ 预注册两段式（判据+脚本骨架先行 commit、exit 2=未跑；实跑 stdout 落 logs/reports 并 commit）。
- 2c review/signal_eval.py：定位以"momentum 无 signal 表 score 行"为由的弃权/理由文本（grep `score 行|弃权`），理由失实→修正文案并重算该维（只读）；若结论变化仅输出新 verdict 供人工拍板，绝不改 config。附前后 verdict 对照落 logs/reports/。

**D-3 三态出口**（signals/rotation.py、rotation_x.py、hedge_pair_research.py、macro_ratio_research.py）
- 增 Gate0 分辨率前置（信号数/白名单非空月低于预注册下限 → `INSUFFICIENT-DATA`，退出码 3，不进 PASS/FAIL 二元）；已有 H0 无分辨率短路改为同标签。
- 策略库 §10.1/§10.5 追加更正说明：R1/X1/X2 由"FAIL"更正为"无分辨率未裁决（原判据与数字保留）"；§10.5"巧合非缺陷"改写为"同日覆盖序工件（未预注册）+选择性耦合可能+n=1 不可分辨"；R1/M1 结论旁注"预注册不可复核（判据与结果同 commit）"。
- DoD：`git diff` 审查确认阈值零变动；新增三态单测（合成数据触发 exit 3）。

**D-3a/b/c 批次3 清债项 DoD**（每项正反用例）：
- 3a：failsafe 挂 catchup 专属分支（9:14~9:30 且当日 premarket 已跑过 → 调 premarket_failsafe；幂等）；备份幂等（目标存在→跳过并记"已存在"，体检结果独立落盘不被吞）；`emergency_pending_skip` 进 decide 快照清洗+runner 剥离两处名单；postclose 增过期单 sweeper（run_date<今日且 approved → expired+清 pending 文件+notify）；recorder 复用 flock；停牌拒单（快照 volume==0 或当日无 bar → 拒单 fail-closed）；测试 env 强制赋值；run_all 注入 AGSICKLE_LOG_DIR。
- 3b：audit 豁免判据与 recalc 写入口径对齐（消除 40 行 pct_out_of_range 永久报），生产 40 行 UPDATE 不执行只留脚本；`index_valuation` 加 `valuation_mode` 列（建表/迁移函数+临时库测试；生产加列待授权），bundle 对 fallback 行降级披露；profile_verdict 读取处加"universe 行数<N（建议 300，待拍板）→ 判据弃权"守卫；audit 与 recalc 的 tx_pct 判据阈值统一；movers/热门池候选加 pct 自洽预检；冷却窗命中首次触发 notify；catchup 步骤1 补"note 含价格滞后且该日 bar 已到"的跨日愈合分支。
- 3c：技术方案.md:206/决策策略与工作流.md:16/README.md:162 三处漂移清扫（统一 W-D6 权威口径 +17.98%/−47.78%/Calmar 0.376 FAIL + momentum 现状）；批次0 报告回填配对复算证据（剔除 146 畸变票后 Calmar 0.249→0.074，qfq 裁决不阻塞选型结论）；批次3 报告同步 §10.5 措辞；技术方案 §5.5 P6 验收分"流程 KPI/策略 KPI"两栏（策略 KPI 标注"样本不足无从谈起"，补验收窗口起止日待拍板）；risk/engine.py:448 rule_price_limit docstring 与实现对齐；ai/bundle.py 缠论 note 增"消失类 29.78%、超额未检验（Gate3 未跑）"并把"重画率 FAIL"改"机械实现因果性不足"。

## §3 批次编排（多 agent 分工）

```
批次0（今日即做，09-24 收盘前必须落地）——串行：
  主会话实施 0a helper → 0b/0c/0d 可 fan-out 3 个并行 agent（文件域不相交：
  blacklist+engine测试 / limit_halt / postclose+catchup+weekly测试）→ 主会话
  跑 run_all → 单 commit。
批次1（∥ 批次2/3）：1 个 agent 只改 qfq 方案文档 + 只读失真排序脚本。
批次2（∥）：1~2 个 agent：2a+2b（文档/CONSTRAINTS）与 2c+D-3（signal_eval+研究脚本+策略库）分治。
批次3（∥ fan-out 3 个 agent）：a 执行域清债 / b 数据域清债 / c 文档域清债。
  与批次1/2 并行时注意文件域：批次3b 碰 data/audit.py、data/macro.py、ai/bundle.py；
  批次3c 也碰 ai/bundle.py 与 docs —— 3b 与 3c 的 bundle.py 改动合并给 3b 先行、3c 只碰 docs。
批次4（今晚 15:30 后）：只读核实 recorder 首 tick（§5 命令块），主会话或人工执行。
批次5（收尾，主会话）：run_all 全量 → commit 两份 docs（审查报告+本方案）→ §6 回填
  → 向用户汇报 §7 待拍板/待授权清单。
```

依赖约束：0a 先于 0b/0c/0d；批次5 最后；批次1/2/3 之间无硬依赖可全并行。

## §4 新窗口执行命令

主命令（新窗口直接粘贴）：

```
按 docs/修复施工方案-2026-09-21.md 执行修复批。先通读 §0~§3：批次0（日历雷三件 D-0a~D-0d）串行先行，是本批唯一时限项（09-24 收盘前落地）；随后批次1（qfq 方案修订，只改文档）∥ 批次2（治理与三态出口）∥ 批次3（a/b/c fan-out 清债）并行推进，批次5 收尾。全程遵守 §1 守则：.venv/bin/python3、生产库零写入、每项正反用例、已归档研究判据零修改、每批次 run_all 全绿后单 commit、§6 回填、§7 清单向用户汇报待拍板项。
```

批次4 只读核对命令块（今天 15:30 后，任意窗口）：

```bash
cd "<REPO_ROOT>"
tail -n 60 logs/catchup.log
ls -la logs/state/ 2>/dev/null
./.venv/bin/python3 -c "
import sqlite3
c=sqlite3.connect('file:data/market.db?mode=ro',uri=True)
for t in ('minute_snapshot','fetch_log'):
    try:
        print(t, c.execute(f'select count(*) from {t}').fetchone(),
              c.execute(f'select max(rowid) from {t}').fetchone())
    except Exception as e:
        print(t, 'ERR', e)
"
```

判定：`minute_snapshot` 行数>0 且 catchup.log 今天有带时间戳的成功行 → 新调度实弹通过；行数=0 → 按 catchup.log 报错排查（勿先怀疑 recorder 代码，先看 8:00/9:30 tick 是否触发）。

## §5 待拍板/待授权清单（执行完向用户汇报，不得自行定稿）

1. qfq 方案修订稿批准（重拉名单两案并陈：失真驱动 vs core51 全量）。
2. 观察期退出条款数值（X 超额阈值、N 观察日、W+4 周时点）。
3. verdict 宇宙守卫 N=300 阈值。
4. P6 验收窗口官方起止日。
5. 生产库写授权（三件，均可独立批）：① index_valuation 加 valuation_mode 列 ② 40 行 ex日 pct 修复执行 ③（属 qfq 方案）36/51 票 em 重拉+override 表建立。

## §6 回填表（执行窗口填写）

| 批次 | 状态 | commit | run_all | 偏离方案记录 |
|---|---|---|---|---|
| 0 | 完成 | 89df087 | 31/31 | ①D-0a `next_trading_date` 定为 **on-or-after** 语义（由 D-0c「普通周四→周五」验收反推，方案原文"next"含糊，D-0d 周报判据用 td+1 日调用自洽）；②repo 增第四 helper `is_last_trading_day_of_week`（D-0d 三个消费点共用，防 ISO 周判断双份漂移）；③datetime 入参归一（0c 实测 isoformat 带时刻破坏 SQL 比较，已修+回归用例）；④已知遗留：tests/e2e_sprint4_a.py scenario_c failsafe 成交价期望与 P2②"执行日跌停价"语义冲突（先于本批存在、不在 run_all 清单，待裁决，见 §5 补充） |
| 1 | 完成 | 2d43c7e | quick 29/29（本批零代码改动） | 只读脚本落 review/qfq_distortion_rank.py（方案未指定落点）；审查仅点名 5 只，19 只入围以脚本实测全名单复核（≥1.2pp 入围 19 只复现审查计数）；实测失真 top：600809 7.58pp/000333 5.66pp |
| 2 | 完成 | d22e29d | 31/31 | ①Gate0 下限：rotation/x 沿用 hedge H0 量级（月桶中位≥3）、macro_ratio 取 250 事件，rotation/macro 原预注册无下限 → 常量注释标"待预注册复核"；②范围外留档：signal_eval C 维投票谓词 `(C_cur−C_alt)<−C_GAP_SWITCH` 代入负值后与 docstring 语义不符（MDD 更优时仍投切换票，当前被红线压为 hold）——修复属 verdict 语义变更，超出 2c 授权，进 §5 补充待拍板；2c 核实结论：弃权理由**失实**（momentum score 行实测 55,418 行非空），verdict 维持 hold 不变 |
| 3 | 完成 | f6f719b | 31/31 | ①文件域合并按方案执行（bundle.py 归 3b；另将 risk/engine.py docstring 与 catchup 愈合分支并入 3a——与 3b/3c 同文件冲突，同型处理）；②3a/3b 并行均改 data/audit.py（backup 函数 vs check_db 判据区，区域不相交，已合入）；③**施工期间生产库损毁事故**：test_sprint4_d W-D6 用例（无 WHERE 全表 DELETE）经 get_conn 隔离失效于 02:53 覆写生产库 daily_bar/signal/index_daily——本批完工商即从 bak-20260920 文件级恢复（逐表核对零真实损失，527,901 行/816 码），损毁库留证 data/market.db.corrupted-20260921-0253（未入库），根因加固（env 强制赋值+连接保险丝）随本批提交；④40 行修复脚本 DRY-RUN 实测 40 行/16 码与审查一致，**未对生产执行** |
| 4 | 待执行（今日 15:30 后） | 只读无commit | — | 本执行窗口为 09-21 凌晨，未到时点；命令块见 §4 |
| 5 | 完成 | （本 commit） | 31/31（合批终验） | — |

### §5 补充（执行中新增待拍板项，编号接续 §5）

6. tests/e2e_sprint4_a.py scenario_c failsafe 成交价期望（90.0 设计价 vs P2② 执行日跌停价 81.0）与 test_execution.py 既有断言矛盾——e2e 期望如何改（文件不在 run_all 清单，长期无人消费）。
7. signal_eval C 维投票谓词与 docstring 语义不符（见批次3 偏离②）——修谓词还是修 docstring，属 verdict 语义变更。
8. signal_eval 弃权理由复核确认"无 score 行"说法失实但 verdict 不变（hold+红线），A/D 维重算值（−0.0232/+0.0001）是否需要正式归档。
9. 本批 Gate0 新增下限常量（rotation 非空月≥3/信号≥3、macro_ratio 250 事件）为保守暂定值，预注册复核时是否追认。
