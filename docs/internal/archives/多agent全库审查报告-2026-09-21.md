# 多agent全库对抗审查报告（第一性原理批次）— 2026-09-21

审查方式：5 路 fresh-context 对抗审查 agent 并行（数据层 qfq / 策略有效性 / 执行风控调度 / 09-19 以来新增代码回归 / 研究方法论 gate），主会话按 doubt-driven 流程复核归类，对最高影响发现逐条亲验（规则3 接线与 lag 口径、交易日历场景、qfq 方案 override 范围、momentum score 行数、文档漂移三处）。全程只读：零文件修改、零数据库写入（sqlite 全程 mode=ro）、零下单、零 commit。跨模型复核跳过（非交互自主上下文，且未经用户授权调用外部 CLI）。

审查基线：commit a000526（2026-09-21 凌晨）。上一个被独立审计的状态是 e8bfcd1（09-19 Sprint4 完工审查），其间约 19 个 commit（R1+M1、缠论 R3、全量打包批次 0~6、测试修复）为本次首批独立对抗审查对象。

---

## 一、总裁决

**执行链护栏、21 条风控规则、账簿一致性、测试套件（31/31 实测绿）全部通过独立对抗核验，未发现"会立刻产生错误交易/亏钱"级别的执行缺陷。** 三条 P0 均为结构性/治理级：①qfq 等比施工方案存在"落地即白做"的方案缺陷（待用户批准的草案，批准前必须修）；②生产选型判据曾被"判据购物"改写且现行"维持 momentum"配置构成不可证伪棘轮；③研究归档账本把"无分辨率（测不了）"与"FAIL（测了没过）"混记，6 连 FAIL 叙事失真。

另有一组**带确定触发日期的日历地雷**（中秋/国庆长假窗口），最早 09-24 盘后即命中，建议本周内修复。

分级统计：P0×3（结构性）、P1×14、P2×约20（摘要收录）。六条研究线的 FAIL 结论本身经逐条对抗复核**没有一条是错的**——问题全部出在结论强度标签、披露方式与治理框架。

---

## 二、P0（结构性，3 条）

### P0-A 【方案缺陷·阻批】qfq 等比施工方案：override 未列入施工范围，落地后一次 em 断供即被无声冲回加法型
- `docs/qfq等比口径施工方案-2026-09-20.md` 的"做"仅 P1~P4、编排仅 S1~S5；`qfq_source_override` 只出现在 §4.1 作"默认处置"描述（L50/56/58），无 P 编号、无 gate、无 owner；全仓 grep `qfq_source_override` 实现 **0 处**（主会话亲验）。
- 回退路径今天就在：`data/fetcher.py:820-828`——36 票重拉为 em 等比后，下一次 em 不可达期间 `backfill_qfq` 锚点漂移 → `rebrush_qfq_full` 落 tx_qfq → 整票冲回加法型，fetch_log 只留一笔记录，**N1/N3 均为一次性 gate，无任何检查会 FAIL**；"N3 定期复检兜底"无调度主体。
- **叠加洞（名单抓错病）**：36 票名单=形态违例集（gate0b_precheck.json 实读），与实测 20 日动量失真最大的 19 只 watchlist_core 票（600809 7.6pp、000333 5.7pp、000651 5.1pp、000568 4.7pp、000858 4.0pp 等）**交集为空**；方案 §4.2 用"1 日差全库 p50 1.6bp"背书"因子层不受影响"，被高 D 票尾部完全掩盖。
- **建议**：批准该方案前，将 override 表+fetcher 拦截升格为 P0 级前置施工项（带独立 gate 与调度主体）；重拉名单改由"无事件对失真×窗口贡献"驱动或对 watchlist_core 51 票全量 em 重拉；披露指标弃用 p50 改分位+按票最大值。

### P0-B 【治理】生产选型判据曾被随结果改写；现行"维持 momentum"构成不可证伪棘轮
- 时间线（git+产物+risk_event 交叉重建）：09-16~17 回测 pass 判据 v1（年化>基准且 MDD>-25%）下"两个 profile 全不通过"→ 当场改 v2-calmar（`.invalid` 产物 note 自认动机"v1 偏严（两个 profile 全不通过）"）→ 09-17 15:25 momentum Calmar 1.126 pass → **人工把 config 切为 momentum，而当时 gate 框架实际输出 verdict=hold（红线 override）**——切换是绕过判据建议的人工动作。09-18 审查 P0-3 已定性"1.13 系幸存者偏差+涨停漏判(+14pp)+异源合成"；09-19 W-D6 权威重跑：momentum **+17.98%/−47.78%/Calmar 0.376 FAIL，2026 年段 −31.04%（基准同期 −5.46%）**。
- 三个至今未修的治理洞：
  1. **无可证伪退出条件**：全库 grep"观察期/恢复开仓"，除 ADR 附记 B 一句外无任何定义（无时限、无量化条件）。
  2. **红线 override 结构性棘轮**：`C_cur<−30% → verdict=hold` 无条件优先，momentum MDD −47.78% 下该机制永远输出"维持现状"；"切换判据走证据闭环人工确认"在当前机制下数学上不可能产出 switch 建议。09-17 的切换从来不是这套判据的产物。
  3. **A/D 维弃权理由已失实**：signal_eval 弃权理由"momentum 无 signal 表 score 行"——实测 momentum 有 **55,418 行 score**（主会话亲验），回填后理由不复存在，弃权却未复议。
- 客观面（反向检验）：维持 momentum 的**内核是理性的**——三 profile 全 FAIL 下 momentum 是唯一正年化、唯一跑赢基准者，回退 reversal 无数字支持；账户 100% 现金+人工闸门下经济风险趋零。W-D6 口径无打压痕迹且经完工审查逐年逐位独立复现。**不理性的不是"维持"，而是"维持+红线棘轮+无退出条件"叠加成的不可证伪稳态**——该配置在任何未来证据下都输出"继续现状"。
- **建议**：①为观察期补预注册退出条款（如"W+4 周重跑仍 FAIL 或生产超额<X → 停用 profile 空仓等研究线"）；②"pass 判据冻结"成文进 CONSTRAINTS（判据版本随产物固化，改动需新 ADR 且不得在同批结果可见后进行）；③A/D 弃权理由复议；④ADR 附记 B 按自家模板（CONSTRAINTS.md:145）补反方论点。

### P0-C 【方法论】归档账本二元出口把"测不了"记成"没过"——6 连 FAIL 叙事的系统性来源
- R1/X1/X2 的 gate **没有分辨率前置**（对照 R3 Gate1、H0 有前置）：白名单 1/20 月非空时信号腿与基线腿 19/20 个月完全同腿，年化超额比较数学上趋近 0/0，任何阈值下必 FAIL。H0 的 60 触发线在白名单 3/20 月确定时即不可达——"无分辨率"在预注册时点就可预见，仍走完"FAIL 归档"流程。
- 全部阈值无推导入档：+5% 年化超额、|Spearman|≥0.2、重画率 15%（全库 grep 无出处）、触发≥60、clean≥80 均首次出现即冻结。
- **但证据方向全部成立**（见第四节"正面确认"）：六条 FAIL 结论本身无一错误；R3 消失类 30.2%、M1 分辨率 0.09~0.12、H0 分对毛贡献 −2.03% 是真实的负面证据。
- **建议**：三态出口 **PASS / FAIL（有分辨率且为负）/ INSUFFICIENT-DATA（挂起，登记数据条件）**；每线补 Gate0 分辨率前置；每阈值一行推导；稀疏线改事件层配对检验；概念组历史改用外部概念/行业指数全史（akshare 十年级历史，样本约束从"等两年"变"一次取数"，这是 R1/X 复测唯一 cheap 的正道）。

---

## 三、P1（14 条，按域分组；★=有确定触发日期，本周~下周）

### 执行/调度域（最紧急）

**★P1-1 规则3 健康检查将使长假后首个交易日全天交易链瘫痪（含人工 confirm 与应急单）**
`risk/blacklist.py:103-105` lag 按**自然日**计算且 `>3` 即 issue；`execution/runner.py:389` 已接入决策时 RiskContext；`risk/engine.py:204` 非 kill_liquidation 一律 `report_only`；confirm 重跑风控同样拒（runner.py:1238-1243）；应急扫描单连 propose 都进不去（limit_halt.py:236-247）。主会话亲验接线与口径，并澄清现状：stock_info 仅 86 只、当前缺最新 bar 票数=0，**该缺陷现在未触发**。确定触发场景：
- **09-28（周一）盘前**：daily_bar 最新=09-24，lag=4 → 全天所有 buy/sell 降 report_only（当日 bar 收盘后才入库，盘中无自愈）；
- **10-08（国庆后）**：lag=8 同杀；
- **任一 watchlist 票停牌**：从次日起永久 poison 全链路直至复牌。
修法：lag 按交易日历计（"落后最近交易日≥2 个交易日"才报）；缺 bar 腿限定非停牌票；或给 emergency_scan 同款豁免。

**★P1-2 应急卖单 run_date 节假日死穴——09-24 盘后即真实触发场景**
`signals/limit_halt.py:166-178` `_next_exec_date` 只顺延周末；交易日历证实 **09-25（周五，中秋）休市**。09-24 盘后应急单 run_date=09-25 → 休市日 catchup 秒退、failsafe 不运行 → 09-28 failsafe 按 `run_date=today` 查不到、人工 confirm 跨日闸判 expired → **该单作废且 09-28 早上无任何提醒**，唯一恢复是盘后重扫。批次4a③只修了周末一半。修法：`_next_exec_date` 接 `data.trade_cal`，或查询改 `run_date<=today AND status='approved'` 按日历顺延 TTL。

**★P1-3 短周周报永久漏发（本周中秋周即命中）**
周报仅两个触发点均判 `weekday()==4`（postclose.py:363、catchup.py:235）；09-21~24 周（09-25 休市无周五）、09-28~30 周（国庆前无周五）连续两周漏发且无告警。修法：改"当周最后一个交易日盘后"触发。

**P1-4 09:14 failsafe 自动执行/再通知节点在当前调度下不可达**
`premarket_failsafe` 全仓唯一调用点在 premarket.py:110，而 premarket 仅在"当日 bundle 缺失或信号错位"时被 catchup 补跑——正常日 8:30 tick 写完 bundle 后 9:14 节点无人触发。`emergency_direct_exec=true` 承诺的自动执行只在"机器睡过头且 bundle 仍缺失"的悖论场景才可能发生。修法：挂独立触发点（recorder 同款双 gate 后轻量调用，或 catchup 加 9:14~9:30 专属分支）。

**P1-5 备份步骤不幂等，当日第二跑抛错并吞掉体检结论**
`data/audit.py:185-200` check_db 先跑、backup_db 后跑且按天命名，当日第二跑抛 `output file already exists` → postclose 记"FAIL（继续）"，**已完成的体检结果随之丢弃**。生产已发生（pipeline.log 2026-09-20 08:29:09）；catchup 每 30 分钟重跑时备份必失败。修法：`VACUUM INTO` 覆盖语义或存在即跳过。

### 数据域

**P1-6 C4"加法型仅是历史问题"字面为假：增量写入今天仍在产出加法型 qfq，且是每年除权季复发的现役问题**
483 笔全史重刷中 473 笔 source=tx_qfq（仅 10 笔 em）；60 只 watchlist 票 09-19 全部被 tx 加法重锚；em K 线域 09-20 起源级不可达。因子层读 close_qfq（signals.py:115-117），加法型在除权日前窗口段把收益环比放大 p/(p−D) 倍（实测 000001 相对放大 26%）。当前决策窗口实测干净（60 根 bar 内失真>0.5pp 仅 1 非 watchlist 对），但每年 5-9 月除权后 1-2 个月内 watchlist_core 高 D 票的 m5/mom20/ATR%/signal_eval 前向收益系统性带偏（放大率 3.5~5.7%）。

**P1-7 ex-event 日 pct_chg 语义错：40 行 pct_out_of_range 是 recalc 与 audit 互斥的永久残渣**
002271 2024-09-26 除权日官方口径应 ~+10.04%，recalc 按 qfq 总回报比写入 +12.22%（超板）；audit 豁免判据"|qfq环比|≤板幅"对写入值恒假 → 40 行/16 码永久报 problem 且 `audit --fix` 无修法。该 pct_chg 进 bundle/LLM 上下文（"当日涨幅 12.22%"实为含分红总回报）。修法：ex 日 pct 改官方除息参考价口径，或 audit 豁免判据与 recalc 写入口径对齐。

**P1-8 回测宇宙断粮：730/816 票最新 bar 停在 09-11/09-14，无自动补数调度**
86 只 watchlist 全有 09-18 bar（决策不受影响），但 C-TEST-5"pass 必须 full 宇宙"在数据层已不可持续——**下次重跑的"full"只剩 86+零星票**，判据形式有效实质不可执行。修法：universe 增量纳入 catchup 周期，或 verdict 读取处加"universe 行数<N → 判据弃权"守卫。

**P1-9 宏观估值链 fail-open：价格分位写入 pe_pct 列且库内无来源标记**
`data/macro.py:123-139` 5 年价格分位 fallback 写入 pe_pct（pe=NULL），表无 source/mode 列，bundle/webapp 无法区分两种语义；PE 成功 PB 失败时写半行。当前库未触发过（pe NULL 行=0），属机制性风险。修法：加 valuation_mode 列；bundle 对 fallback 行降级披露。

### 策略/方法论域

**P1-10 full 宇宙=2026-09-14 现任中证800 成分回溯 2024-25，对 momentum 是方向性偏差**
universe_member 800 行全部 as_of='2026-09-14'；用"今天赢家的名单"回测昨天系统性抬高 2024-25 段——恰是维持论据的来源年；最少偏差的 2026 段（−31.04%）拍板时未讨论。修法：历史时点成分重跑，或至少做"仅 2026 段"截断对照入 metadata。

**P1-11 qfq 畸变与选型的交叉对照缺失（审查方已代做，方向不利）**
独立配对复算（同简化 momentum 引擎、0.35/0.40/0.25 生产同式、周调仓 Top5、0.15%/边）：全池 ann=11.91%/Calmar 0.249；**剔除 146 只≥5 天畸变票后 ann=3.14%/Calmar 0.074**——加法型畸变在抬高 momentum 读数，剔除后 FAIL 加深。应回填批次 0 报告，作为"qfq 裁决不阻塞选型结论"的证据。

**P1-12 R1/M1 的"预注册"不可由 git 证实**
判据、脚本、结题结果三者同一 commit（dfedcc6）入库，git 无法区分"先冻结后跑"与"跑完写回"；方案 §5 自认 pass 标准数值成文时未经用户最终拍板。全量打包三线（批次 0/1/3/缠论证据）物证链仅 hedge_pairs_current.json 一条 mtime 旁证。结论无需推翻（0.00% 超额对任何合理阈值都 FAIL），但引用应注明"预注册不可复核"。修法：两段式 commit（判据+脚本骨架先行、exit 2=未跑；实跑 stdout 落 logs/reports 并 commit）。

**P1-13 X1/X2"巧合非缺陷"措辞过强：未预注册的同日覆盖序工件**
两轴信号源与 R1 完全同机（rotation_x.py:53 import 复用），R1 白名单 20 个月仅 1 个月非空 ⇒ 两轴先天只能在同月产生信号；`rotation.py:162` 后触发覆盖先触发使有效目标=半导体==当月动量持有组——**若 config 概念列序不同，两腿即不同**；另有"能被检出与恰是动量组"统计正相关通道未排除，n=1 无法区分巧合与结构。gate 裁决成立、作为假设证伪无效。§10.5 已自书"不可分辨非被证伪"，建议批次 3 报告同步改写并预注册同日决胜规则。

**P1-14 实现样本对"正期望"零证据力，当前超额全部来自空仓**
全库仅 2 笔成交（09-14 buy 300750 买在日高区 98% 位置、09-16 止损 −11.3%/净亏 −7,749.23）；账户生涯部署率仅 **6.7%**（30 个交易日 2 天持仓；"空仓两周"说法不准——清仓在 09-16 仅 3 个交易日）；净值超额 +3.36pp 全部来自现金。P6 验收正被"不交易"满足；验收窗口官方起止日两份文档均未写明（若自 08-10 起算已过而未见结题记录）。建议流程 KPI 与策略 KPI 分列，明示策略 KPI 无从谈起。

---

## 四、正面确认（对抗后站得住的域）

- **测试套件**：run_all 实跑 31/31 绿（15.1s exit 0）；tests/ 恰 31 个 test_*.py 与注册清单逐一一致、零缺注册（此前"36 个文件"系把 conftest/fixtures/e2e/net_guard 误计）；无 pytest.skip 静默缩水；抽核断言无同义反复。
- **风控引擎 21 条规则全部可达，无死规则**：与 test_risk_engine 55 用例一一对应；规则 21 修复属实（三条件+扫描器显式比价+三条触发路径接线+6 个专项测试）；规则 20 有真实状态迁移记录；豁免接线正负用例齐全。
- **执行护栏**：skip_gate/confirmed_by 双重剥离属实；`_explicit_price` 无残留；±2% 基准已修为"严格早于执行日最新收盘"；paper 三前置防线+回读全量重放；webapp 127.0.0.1+Host 校验在位。
- **effective_peak 回放守卫 / 应急执行价 min(设计价,跌停价) / 周末顺延**：实现与声称一致，250 行窗口对该口径无边界问题；pc 取不到时回退设计价并留痕 stale_price_exec（保守劣化，不阻断）。
- **批次 6 语义零变化成立**：hedge_pairs/chan_structure 仅 ai/bundle.py 消费，risk/signals/pipeline/execution 零引用；PROMPT_VERSION=2026-09.2 已同步；**提示注入面低**（两节内容全程序生成，无外部自由文本，降级链顺序与声称逐字一致）。
- **研究脚本只读成立**：全部 mode=ro 或零 sqlite3 调用；批次 0/1/3 生产库零写入属实（daily_bar 无 open_qfq 列、527,901 行、bak-20260920 在位）；⑩ 的 audit --fix 路径收显式连接，不可能误指生产库。
- **账簿四方自洽**：trade→现金→portfolio_state→日报，−7,749.23/0.7749% 逐位吻合。
- **盯市/止损/成交价格源用 raw close**，不受 qfq 口径污染（C1 的止损/盯部分成立）。
- **数据基本面**：volume 量纲/OHLC 形态/负 qfq 价 0 违例；legu 统计日期与 bond 新鲜度闸真 fail-closed；fetcher 防半根 bar、empty_today fail-loud、熔断落盘跨进程恢复均属实。
- **六条研究 FAIL 结论本身无一错误**：R3 最硬（消失类 30.2% 口径无关）、M1 真分辨率不足、H0 短路合规且三处后续引用均诚实保留"无分辨率"原义、R1 白名单代码无符号错误（1/20 是策略性质非 bug）、W-D6 经逐年逐位独立复现。
- **生产代码无硬编码业务日期地雷**（a000526 同类模式 grep 命中均为注释/docstring；trade_calendar 覆盖 2026 全年且中秋/国庆缺口正确）。

---

## 五、P2 摘要（20 项，全文见各路审查原始记录）

数据域：141 行/9 码残留复核成立但两套判据并存、600016 已进 movers 池缺预检；冷却窗语义为"保留窗"非"回滚弃窗"（ADR 已披露），且命中仅 log.warning 无 notify；回滚 claim 与实现不符属已披露收窄。执行域：`emergency_pending_skip` 不在剥离名单（影响有界：允许按跌停价卖=应急单合法语义，但违反"提权键只能服务端注入"声明）；停牌票可被模拟成交（无显式停牌防线，volume==0/无当日 bar 应拒单）；15:05 TTL 惰性过期无 sweeper；recorder 与 catchup 无互斥（WAL+busy_timeout 兜底）；测试流量污染生产日志；crontab 无失败外呼通道（旧调度沉默失败 216+20 行为前车之鉴）；快照 order 匹配按 code+action 取第一个，同日同票双单可错配。工程域：⑬愈合分支只覆盖当晚，跨日迟到数据无愈合；测试 env 用 setdefault 非强制覆盖（shell 残留变量可击穿隔离）；rule_price_limit docstring 与实现不一致（声称有事件留痕兜底实则只查 flag）；bundle 终档溢出直写（既定设计，记录知悉）。披露域：缠论 LLM 可见 note 缺量化（应补"消失类 29.78%、超额未检验（Gate3 未跑）"，"重画率 FAIL"应改"机械实现因果性不足"）；文档漂移三处（技术方案.md:206 仍 ✅calmar 1.13 pass、决策策略与工作流.md:16 仍称 reversal_lowvol 默认、README.md:162 仍引 31.81% "仍达标"）——主会话已亲验三处原文。

---

## 六、未来两周日历地雷时间线

| 日期 | 事件 | 关联发现 |
|---|---|---|
| 09-21（今天，周一） | 新 crontab（catchup */30 + recorder */5）**首次实弹**；09:30-15:00 recorder 首个交易日 | 审查时点（凌晨）recorder 零 tick、catchup.log 历史零成功记录——今天收盘后须核对 minute_snapshot 行数与心跳 |
| 09-24（周四） | 若盘后触发跌停应急单 | P1-2：run_date=09-25（休市）→ 单据作废 |
| 09-25（周五，中秋） | 休市；批次7 automation-7fbfcdf7 15:40 触发 | P1-3：本周周报必漏；批次7 体检对象实为 09-21~24 四个交易日 |
| 09-28（周一） | 长假后首个交易日 | **P1-1：lag=4 → 全天交易链瘫痪（含人工 confirm）**；应急单跨日闸 expired |
| 10-01~10-08 | 国庆长假 | P1-3 国庆周周报漏发；**P1-1：10-08 lag=8 同杀** |

**最小修复建议（本周内，全部为小改动）**：P1-1（lag 按交易日历）+ P1-2（_next_exec_date 接 trade_cal）+ P1-3（周报触发改最后交易日）——三条都是 10 行量级修复，但不修则 09-28 全天不可交易、应急链在长假窗口失效。

---

## 七、建议行动优先级

1. **立即（本周）**：修 P1-1/P1-2/P1-3 三个日历雷；今晚收盘后核对 recorder 首 tick（minute_snapshot 行数、recorder_heartbeat）。
2. **批准 qfq 方案前**：按 P0-A 修方案（override 升格 P0 前置项+名单重定+披露指标改口径），再判据冻结开工。
3. **治理**：P0-B 四件套（观察期退出条款预注册/判据冻结成文/A-D 弃权复议/附记 B 补反方论点）；P0-C 三态出口+Gate0 分辨率前置+阈值推导入档。
4. **下个清债批**：P1-4~P1-9 与 P2 清单按域分批；文档漂移三处一次性清扫。
5. **研究线**：R1/X 复测走"外部概念指数全史"路线（cheap 正道）；缠论披露语按 P2 建议改写；全部研究引用注明预注册可复核性。

## 八、无法核实项（如实留档）

- 今天 08:00 crontab 首次实弹结果、09:30 后 recorder 真实落 tick（审查时点盘前）。
- em/tx 源当前在线状态与"em K 线域源级不可达"的根因（外部服务，审查零网络）。
- 09-19 凌晨 04:17 一批 483 笔全史重刷的触发入口（fetch_log 无 caller 字段，疑手工批跑，审批痕迹不在库内）。
- 40 行 pct_out_of_range 的"官方除权参考价真值"（库内仅 tx 单源）。
- W-D6 精确口径逐位复现（避免覆写 logs/backtest_result.json 未重跑官方引擎；独立复算用简化引擎只证方向）。
- R1/M1 及全量打包三线实跑是否晚于判据撰写（git 物证链缺口，见 P1-12）。
- ZCode automation（09:14 节点、automation-7fbfcdf7）在应用层的真实注册状态（项目文件内无实体）。

---

*审查主体：5 路 fresh-context 对抗 agent（数据/策略/执行/回归/方法论），主会话 doubt-driven 复核与亲验。原始逐条证据（file:line、SQL 输出、git 考古）保留于各路审查记录；本报告为归类综合。发现与 09-18 报告重合者（判据购物、momentum pass 虚假）标注为"确认+深化"，新发现均给出独立证据。*
