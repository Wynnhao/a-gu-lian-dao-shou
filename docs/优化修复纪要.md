# 优化修复纪要（2026-09-13 多维审查 + 2026-09-16 Sprint 1）

> 2026-09-13 由 5 个维度（数据 / 风控执行 / 策略信号 / AI 决策与流水线 / 工程产品）的
> 并行审查产出约 40+ 项改进点，本轮全部修复。本文是修复清单与验证方式；
> 各发现的原始证据见当日多维审查讨论纪要。
>
> 2026-09-16 Sprint 1（基于 `docs/策略库审查报告-2026-09-16.md` P0-1/P0-2/P0-3 + 6.4 落盘）落地 5 项：
> signal_eval 落盘 + profile 投票 + 规则 20 因子拥挤熔断 + 规则 21 跌停封单应急 + quotes 字段扩。

## 一、正在发生的实害（P0，已修）

### 数据层（实测证实的存量污染）
| 问题 | 修复 |
|---|---|
| daily_bar.volume 量纲混杂（东财=手，腾讯兜底部分=股，差 100 倍） | 入库时按 amount/close 隐含股数逐行归一为「手」；`.venv/bin/python3 /-m data.audit --fix` 修复存量 |
| 不复权 + pct_chg 由 close 重算，除息日被当暴跌（实测 600519 虚低 2pp） | 优先用东财官方「涨跌幅」列；腾讯源用库内前收盘推算；新增 close_qfq 前复权列，因子层优先复权价 |
| 午休时段半根 bar 污染（launchd 30min 触发命中 is_trading_time 盲区） | 新增 `_market_data_window`（9:15-15:05 连续保护窗），覆盖集合竞价/午休/收盘尾差 |
| fetch_log 全记 ok，降级口径不可见 | daily_bar 加 source 列，fetch_log 记录实际命中源 |
| SQLite 三方并发写无防护 | get_conn 统一 WAL + busy_timeout=15s |
| 无备份 | postclose 每日 `VACUUM INTO logs/backup/`（保留 30 份）；`.venv/bin/python3 /-m data.audit --backup` 手动 |
| 无交易日历，节假日照常跑 | 新增 data/calendar.py（新浪交易日历缓存），catchup/风控/盯市引用；日历不可用退化 weekday |
| 东财封禁无退避熔断（fetch.log 实测连续 Connection aborted） | 统一 call_ak：指数退避重试 + 连续 5 次失败冷却 30 分钟 |
| 入库前零校验 | 新增 data/audit.py：OHLC 关系 / 停板边界（按板块） / 量纲自检，`--fix` 修复量纲 |

### 风控执行（人工闸门自身有洞）
| 问题 | 修复 |
|---|---|
| `confirm --price` 绕过全部价格类风控 | 最终执行价确定后重跑价格保护 + 涨跌停校验（price_recheck），不过保留 pending 可重试 |
| readback 失败后重跑 confirm 二次成交 | 失败置 status=executed_unverified（不可再 confirm）；trade 加 decision_id 唯一索引 + PaperBroker 成交前查重 |
| kill 残仓（T+1 不可卖/卖出失败）停机期锁死 72h | 递延写 kill_liquidation_pending；停机期放行清算卖单；盘前 resolve_liquidations 自动补清算 |
| kill 停机期靠 `rule LIKE '%kill%'` 推导、无法人工恢复 | logs/state/kill.json 权威状态 + `runner.py kill resume/extend/show` CLI |
| pending 无 TTL，昨日决策今日可执行 | pending 写 valid_until（当日 15:05）；confirm 校验 run_date=今日，跨日置 expired |
| 无单票止损/概念集中度/流动性约束/同票往返 | 新增规则 16-19（config.risk：stop_loss_pct=8%、max_concept_weight=45%、max_amount_share=1%）；止损盘中扫描报告+通知 |
| 涨跌停价 round() 银行家舍入差 1 分钱 | Decimal ROUND_HALF_UP（风控与撮合同口径） |
| paper 零滑点/可无限吃成交量/涨停也能成交 | slippage_bps=10、volume_participation_cap=1%、sim_limit_halt（config.execution，测试可环境关闭） |
| risk_event 只写库不通知 | 新增 risk/notify.py：kill/回读失败/成交失败/pending 生成 → macOS 通知中心 + 可选 webhook（config.notify） |
| cron/人工/看板并发执行 TOCTOU | propose/confirm/reject fcntl 文件锁 + webapp 串行化 |
| mode=ui 只有文档没有代码 | runner 启动硬拒绝（AGSICKLE_ALLOW_UI=1 可演练绕过） |

### AI 决策与流水线（实测证实的断链）
| 问题 | 修复 |
|---|---|
| 决策 run_date 口径断裂：日报「决策回顾」永远查空（实测 2026-09-11 报告） | 统一 run_date=今天（预期执行日），evidence_date=最新交易日；bundle/decide/propose_db/午评/日报全链对齐 |
| 同批决策重复入库 5 次（实测 decision#5-19） | save_decisions 内容级幂等跳过（不做 DB 唯一键，保留午评同日同票合法增量） |
| 决策→结果闭环缺失，置信度无法验证 | decision 加 trade_date/model/prompt_version/t1_ret/direction_hit；盘后回填；日报决策表带「次日实际/方向」；周报新增「决策质量」章节 |
| 校验失败整包放弃、无现场 | 原始输出+原因清单落盘 decision_raw_<ts>.json，可修正后重跑 |
| launchd 看门狗失效 13+ 次静默（实测 catchup.log） | catchup 写心跳文件；盘前体检心跳滞后>3h 告警+留痕+通知 |
| 决策重复/单一 | catchup 加文件锁单实例；交易日历门秒退；信号新鲜度校验；回补窗口 10→25 + 窗口外警告 |
| bundle 信息密度低（33KB 中 29 行 "PASS -"、宁德 5 条同事件新闻） | PASS 黑名单折叠、数据质量只列滞后票、新闻读取侧同事件去重+相关性排序、md 超 45k 字符逐级降级新闻正文 |
| LLM 读 md 看不到 ATR/换手/均线 | md 信号表补齐（5日/20日动量、ATR占比、换手分位、站上MA60、价格口径） |
| 卖出决策缺依据：持仓无现价/浮盈/权重 | bundle 持仓补现价/市值/浮动盈亏/当前权重 + 累计已实现盈亏 |
| watch 可带 target_weight>0 | validate 强制 hold/watch 权重=0 |
| 午评 --now 回放污染正式目录、黑名单硬编码 | 回放产物隔离 logs/session/test；黑名单名单动态渲染；midday_bundle.json 落盘 |

### 信号策略（score 方向性与回测虚高）
| 问题 | 修复 |
|---|---|
| 20 日动量正向 0.40 与策略库自有结论（中期动量 IC 为负）矛盾 | score 双 profile：默认 reversal_lowvol（5日反转0.40+低波动0.35+低换手0.25，落地策略库 Top1）；momentum 保留可切（RSI 二值改连续衰减）；config.signals.profile |
| 回测四重虚高（事后票池/未复权/涨停可成交/样本内挑参） | 可成交口径（封涨停跳过/停牌ffill>5日剔除/成交额5000万地板）；成本统一（佣金+印花+滑点10bps）；双 profile 对比+分年度+滑点敏感性；移除挑参循环 |
| signal 表留史从不评估 | 新增 review/signal_eval.py：RankIC/score 五分位分层/入池后收益胜率，周报引用（样本不足显式标注） |
| movers 盘前全市场口径混写/合并只换 reason 不换 strength | 盘前 market_mode=False；合并同步 strength；dynamic_pool 加 mode 列 |
| N/C 前缀误杀长期票、"退"字票漏网 | 按上市日 <10 天判次新；增加退市整理过滤；黑名单票标注 blacklist |
| hot_theme「明显高于基线」docstring 承诺未实现 | 补基线突增条件（≥2× 此前 5 日日均，config theme_vs_avg） |
| 深市指数前缀推断错判 | 显式映射表 |

### 工程（可信度与可复现）
| 问题 | 修复 |
|---|---|
| server.py（唯一写入口）零测试 | 新增 tests/test_webapp.py 冒烟：穿越/Host 403/Origin 绕过/415/413/404 实请求 |
| Origin startswith 可被 127.0.0.1.evil.com 绕过、GET 无 Host 校验 | 精确 hostname 比对 + GET 统一 Host 白名单 |
| HealthPanel 自递归 bug 且审查报告声称"已挂载"失实 | 修复并真正挂载报告页；建议复核审查报告其他"已修"声明 |
| Origin 之外：report 404 语义、equity_curve N+1、配置反复读盘 | 全部修复；黑名单/健康检查复用 risk.blacklist 单一来源 |
| 无 requirements/解释器硬编码 Xcode 路径 | requirements.txt 锁定；deploy/run_catchup.sh 统一入口（venv 优先）；plist 改调 wrapper |
| 日志无限增长 | 全部 RotatingFileHandler（5MB×3） |
| 119 用例手工逐个跑 | tests/run_all.py 聚合入口（9 文件 136 用例） |
| 决策理由无证据跳转、手机横滚不可用 | 决策展开区「查看当时输入包」跳转；App 允许横向滚动降级 |
| README 未提交、单 commit | 已补提交；此后按模块粒度提交（本次修复分 5 个 commit） |

## 二、修复后回测实测（2026-09-13，口径修复后）

可成交口径 + 统一成本（佣金+印花+滑点10bps）下的双 profile 对比（同一 30 只票池，2024-01 起）：

| profile | 年化 | 最大回撤 | 达标(ann>基准 且 mdd>-25%) |
|---|---|---|---|
| momentum | +35.5% | -32.2% | 否（回撤超限） |
| reversal_lowvol | -8.0% | -36.2% | 否 |
| 沪深300 | +11.7% | — | — |
| reversal 滑点敏感性 | 5bps:-5.0% / 10bps:-8.0% / 15bps:-10.9% | | |

**解读（两个方向都要看）**：
- 旧回测 31.8% 年化是"30 只事后挑选的上涨票 + 未复权 + 涨停可成交 + 无滑点"合成的虚高；
  修掉后 momentum 也只有 +35.5% 且回撤 -32% 超验收线——旧数字不可信已被证实。
- reversal 在本票池表现差，与策略库"全市场短期反转显著"并不矛盾：票池本身是按
  "后来涨得好"人工挑的（幸存者偏差最利冲动量），反转因子在全市场有效 ≠ 在
  挑选后的高 beta 票池有效。
- **决策**：默认 profile 暂保留 reversal_lowvol（策略库研究先验 + P6 模拟盘本身就是
  为验证而设）；切换只需改 `config.json → signals.profile: "momentum"`。
  signal_eval 积累 100+ 样本后以实证 IC 定夺。

## 三、明确的遗留项（不在本轮范围）

> **2026-09-14 进展**（策略库审查落地，详见 docs/策略库.md §10）：
> 遗留项 1（中证800 宇宙）已由 `data/universe800.py` 经腾讯源回补（东财仍封禁）；
> 遗留项 2（signal_eval 实证校准）已接入 signal 全史回填（`.venv/bin/python3 /-m signals.signals --backfill`，
> 18,946 样本），score 已改回策略库 2.1 截面口径，profile 切换判据成文（review/signal_eval.profile_verdict）；
> 遗留项 4 的复权数据部分已解决——腾讯源 qfq OHLC 回填全库（东财解封后可再跑一次统一口径），
> 并新增 high_qfq/low_qfq 列修复 ATR 混口径问题。
> 遗留项 3（LLM 盘后自我复盘）、5（前端工具链）仍开放。

1. **中证 800 回测宇宙回补**：东财接口当前封禁中，800 票日线回补待接口恢复后执行
   （backtest 已支持任意 daily_bar 票池，宇宙数据到位即生效）。
2. **signal_eval 实证校准 score 权重**：signal 表历史尚短（库 09-12 才建），工具已就位，
   积累 100+ 样本后按 IC 证据微调权重；当前 reversal_lowvol 默认值来自策略库研究先验。
3. **LLM 盘后自我复盘**：复盘输入包/结果落库通道已就绪（decision.review 列、决策质量章节），
   复盘对话本身属每日 ZCode 会话职责，需在 cron 提示词中加入该环节。
4. **个股基本面数据**（日频 PE/PB、ST 标记）：模拟盘非硬依赖，实盘化前必须补。
5. **eslint/vitest 前端工具链**：建议引入，本轮未加。
6. **一次性人工操作**（代码无法代劳）：
   - 看门狗授权（系统设置 → 完全磁盘访问），重启看门狗：双击 `看门狗开关.command`；
   - 存量数据修复：`.venv/bin/python3 /-m data.audit --fix`（量纲归一）后跑 `.venv/bin/python3 /-m data.audit` 复核；
   - 东财解封后跑一次 `.venv/bin/python3 /data/fetcher.py`（自动回填 close_qfq 前复权列）。

## 三、验证方式

```bash
.venv/bin/python3 tests/run_all.py    # 9 文件 136 用例，全离线
.venv/bin/python3 -m data.audit       # 数据体检（修完存量应为 0 或仅余无法自动判定项）
.venv/bin/python3 risk/engine.py demo # 风控 24 场景冒烟
.venv/bin/python3 webapp/server.py    # 看板回归（新增数据健康面板/决策跳转）
```

回测对比：`.venv/bin/python3 signals/backtest.py` 输出 momentum vs reversal_lowvol 双口径
（含可成交约束与统一成本），对照 logs/backtest_result.json。

## 四、Sprint 1 验收条目（2026-09-16）

### 任务 1 · signal_eval 输出落盘 + bundle 接入（6.4）
- `review/signal_eval.py::main()` 落盘 `logs/signal_eval/YYYY-MM-DD.json` + `latest.json`（Path.replace 原子覆盖）
- `ai/bundle.py::_profile_verdict_latest()` 读取 latest.json，注入 `bundle["profile_verdict_latest"]`，markdown 渲染 "Score Profile Verdict（v1.4）" 段
- `pipeline/premarket.py` 步骤 6.7 在 compute_all 之后、write_bundle 之前调 `evaluate() + _persist_latest()`（保证 bundle 读到当日最新）
- **测试**：tests/test_review.py::test_signal_eval_persists_latest（新）

### 任务 2 · profile 切换判据 v1.4（B+C 两维 + MDD 红线 override）
- `review/signal_eval.py::profile_verdict` 重写：B = 年化超额 vs HS300（阈值 −10pp），C = MDD（阈值 −5pp）；多数决 2/2 才切
- MDD < −30% → 无条件 hold + 写 risk_event（rule='profile_verdict_red_line'，同日去重）
- A（RankIC h5）与 D（五分位多空价差）因 momentum 无 signal 行**弃权**，写入 verdict.abstains 留痕
- 切换永远人工确认：不自动切 `config.signals.profile`（保留 v1.3 注释与文档 §7 双锁定）
- **测试**：tests/test_review.py 新增 5 case（双劣 switch / 单劣 keep / 红线 override / 弃权 hold / 兼容 v1.3）

### 任务 3 · 腾讯报价字段扩（规则 21 配套）
- `data/quotes.py::_fetch_tencent` 解析 f21/f22/f42/f45/f46：ask1_price / ask1_vol（手）/ float_mv（元）/ limit_up / limit_down
- 字段偏移沿用本函数"市场类型 +1"约定（与 f[33]/f[34] 同款），不动 high/low 现状避免破坏 test fixture
- 审计文件 `logs/quotes/YYYY-MM-DD.jsonl` 自动镜像新字段（_audit_snapshot 不改）
- **测试**：tests/test_quotes.py 新增 3 case（旧 fixture 不漏 / 新字段解析 / 空 ask1_vol=None）

### 任务 4 · 规则 21 跌停封单应急
- `risk/engine.py::rule_limit_halt_emergency` 三条件 AND（条件② 缺数据静默跳过）：卖价=跌停价 + 死封（ask1_vol×100/float_shares > 3%）+ 浮亏≥止损线
- 触发动作：v.emergency_pending 挂事件 → check() 调 record_event 写库；规则 14 通过 decision["emergency_pending_skip"] 豁免（flag 必须由规则 21 自己写）
- 规则调度顺序：规则 21 必须在规则 14 之前（先写豁免 flag，再让规则 14 检查）
- 默认走 confirm 闸门（plan 4.3 选项 A：不绕过人工）；如要严格按审查报告"绕过 LLM"直写 orders，需另启 ~0.3d
- **测试**：tests/test_risk_engine.py 新增 4 case（强封单触发 / 缺数据触发 / 弱封单不触发 / 普通 sell 仍拒卖）

### 任务 5 · 规则 20 因子拥挤度熔断
- `signals/signals.py::_write_factor_crowding` 在 compute_all 末尾按月分桶滚动 IC（μ60/σ60），落盘 `logs/signal_eval/factor_crowding.json`
- 阈值：`μ60 < 0.005 且 σ60 > 0.02` → crowded=True
- `risk/engine.py::rule_factor_crowding` 读取，buy 单 target_weight > 5% 自动压回（按 equity 重算股数，整手规整；不足一手则拒）
- `ai/bundle.py` 注入 `bundle["factor_crowding"]`，markdown 渲染 "Factor Crowding（规则 20 熔断态）" 段
- `_OUTPUT_RULES` 加规则 8：拥挤时 buy 需 confidence ≥ 0.7
- **测试**：tests/test_signals.py 新增 3 case（合成数据落盘 / 空信号表 / 规则 20 压权重 + 不足一手拒）

### 回归验证

```bash
# 任务 3 测试
.venv/bin/python3 tests/test_quotes.py            # 9/9（含 3 新增）
# 任务 1+2 测试
.venv/bin/python3 tests/test_review.py            # 21/21（含 6 新增）
# 任务 4 测试
.venv/bin/python3 tests/test_risk_engine.py       # 43/43（含 5 新增）
# 任务 5 测试
.venv/bin/python3 tests/test_signals.py           # 27/27（含 3 新增）
# 全套回归（16 文件 208 用例，全离线，全绿）
.venv/bin/python3 tests/run_all.py
```

### 待 Sprint 2 处理（已在原计划划线）

- ❌ `signals/profile()` 二元对偶逻辑暂不加 `reversal_lowvol_v2`（仅两个 profile）
- ❌ signals.py reversal_lowvol 权重降级（任务 5.3 暂挂，等 P1-2 IVOL+MAX 落地后再审视）
- ❌ 规则 21 直写 orders 不走 confirm 的激进路径（plan 4.3 选项 B）
- ❌ P1 全部（市场宽度 / IVOL+MAX / 国债+ETF / 业绩预告）

## 五、Sprint 2 验收条目（2026-09-16）

### 任务 1 · P1-3 国债收益率 + ETF 份额 · regime 第 5/6 路并联
- `data/macro.py` 加 `fetch_bond_yield()` / `fetch_etf_share()`，接 `call_ak` 熔断（macro.py 当前缺此机制，Sprint 2 补洞）
- 新表 `index_bond_yield` / `index_etf_share`（DDL + `_MIGRATIONS` 幂等 ALTER）
- `risk/regime.py` 加 `_compute_bond_yield_modifier`（乘子 0.8）+ `_compute_etf_tier_shift`（cap 上下浮一档）
- `compute_regime` 末尾并联两路到 `caps`，保持"min 取最保守"语义
- bundle 注入 `bond_etf_signals`，markdown 加 "## 国债 + ETF 旁路" 段

### 任务 2 · P1-4 业绩预告关键词事件通道
- 新建 `signals/earnings.py`，内置 `EARNINGS_POS` / `EARNINGS_NEG` 关键词表
- `compute_earnings_events(conn, days, min_net_score)` 写 `news_earnings` 表，samples 保留前 3 条标题
- `ai/bundle.py` 注入 `earnings_events_latest`；markdown 加 "## 业绩预告事件（近3日）" 段
- `_OUTPUT_RULES` 加第 9 条：net_score ≤ −2 禁止 buy
- `pipeline/premarket.py` 步骤 6.5 后加 6.6：调 `earnings.refresh()`

### 任务 3 · P1-1 市场宽度/情绪模块
- 新建 `data/breadth.py`：akshare `stock_zt_pool_em` 主源 + 腾讯 `quotes.ask1_price==limit_up_price` 兜底 + 新浪网页爬虫 fallback
- 新建 `signals/breadth.py`：`compute_breadth_factor()` 读 `breadth_daily` 表，breadth_composite < −2 → override_cap=0.1（极端避险）
- 新表 `breadth_daily` + DDL + 熔断接入
- regime 第 7 路并联
- bundle 注入 `breadth_composite`

### 任务 4 · Sprint 1 任务 4.3 选项 B 收尾（skip_gate 标志）
- `risk/engine.py::rule_limit_halt_emergency` 末尾加 `decision["skip_gate"] = True`
- `execution/runner.py:580-602` 改 elif：`manual_gate=true and not decision.get("skip_gate")` 才走 pending
- 走 skip_gate 路径时 `confirmed_by="emergency_rule21"` 落审计
- **默认不启用**：skip_gate 标志只在规则 21 触发时由风控层自己写入，AI 无法绕闸

### 任务 5 · Sprint 4 文档清理
- 删 4 条：§1.3 R-Breaker / §2.3 CH-3 / §5.1 双低转债 / §5.2 网格
- 暂缓 3 条：§2.2 小市值 / §4.2 龙虎榜 / §4.3 北向（降级为 LLM 背景信息）
- 重写 §9 落地建议与审查报告 P0/P1 路线图对齐
- 加附录变更记录

### 附任务 · P1-2 IVOL + MAX(5) 残差类因子
- `signals/factors.py` 新增 `ivol(stock_close, mkt_close, window=20)` 与 `max_ret_bali(close, window=20, top_k=5)`
- `signals/signals.py::_factor_from_bars` 增加 `ivol_20d` / `max_ret_5_20d` 字段
- `_score_cross_section` 加占位（暂不接 v2 profile，Sprint 3 视 IC 累积再接）
- 数据依赖：HS300 已 2113 行，无需补指数

### 与审查报告的关键修正

| 报告假设 | 项目现状 | Sprint 2 修正 |
|---|---|---|
| P1-1 akshare 端点低成本 | em 09-15 已全天封禁 | em + tx + sina 三档兜底 + call_ak 熔断 |
| P1-3 macro.py 低成本扩数据 | 未接 call_ak 熔断 | Sprint 2 一并补熔断机制 |
| Sprint 1 4.3 选项 B 需新写代码 | runner.py:586-602 else 分支已存在 | Sprint 2 是"启用"而非"新写" |
| P1-2 中证800 宇宙待解封 | 腾讯源已可用 | IVOL 直接用 HS300（2113 行已有） |

### 回归验证（2026-09-16 实测，18 文件 242 用例全绿）

| 测试文件 | 结果 | Sprint 2 增量 |
|---|---|---|
| tests/test_ai_pipeline.py | 18/18 | +1（bundle 注入 bond_etf_signals） |
| tests/test_audit.py | 5/5 | — |
| tests/test_breadth.py | 6/6 | 新建（read/override/em mock/三档全失败） |
| tests/test_execution.py | 28/28 | +1（skip_gate 直写路径 e2e） |
| tests/test_fetcher_breaker.py | 3/3 | — |
| tests/test_macro.py | 8/8 | 新建（bond/etf 写入+兜底+signals 读取） |
| tests/test_market.py | 7/7 | — |
| tests/test_movers_hot.py | 7/7 | — |
| tests/test_news_macro.py | 7/7 | — |
| tests/test_pipeline.py | 3/3 | — |
| tests/test_pipeline_full.py | 6/6 | — |
| tests/test_quotes.py | 9/9 | — |
| tests/test_regime.py | 15/15 | +4（国债乘子 ×2 + ETF 档位 ×2） |
| tests/test_repo.py | 11/11 | — |
| tests/test_review.py | 21/21 | — |
| tests/test_risk_engine.py | 45/45 | +2（skip_gate 标志 ×2） |
| tests/test_signals.py | 38/38 | +5（earnings ×3 + IVOL/MAX ×5 中本 sprint 部分） |
| tests/test_webapp.py | 5/5 | — |

实现备注（与计划的三处偏差，均已实测验证）：
1. `_fetch_breadth` 三档兜底循环改为模块属性延迟查找——否则测试 monkey-patch 单档源不生效（for 循环捕获的是定义时函数对象）。
2. tx 兜底在"无代码清单 / 行情全挂"时抛错而非返回全 0 dict——避免落一行假数据进 `breadth_daily`。
3. macro `fetch_bond_yield` / `fetch_etf_share` 加可选 `conn` 参数（生产走 `get_conn()`，测试注入 :memory:）。

### 待 Sprint 3 处理

- ❌ reversal_lowvol_v2 profile 接入（P1-2 因子已算 + signal.signals JSON 已写入，等 Sprint 3 视 IC 累积人工切 profile）
- ❌ signals.py 权重硬编码 0.40/0.35/0.25 调整（Sprint 1 任务 5.3 已由 rule_factor_crowding 动态接管，无需改硬编码）
- ❌ backtest_full.py（生产口径回测，审查报告 §6.3）
- ❌ P2（iVIX/期权 PCR、概念板块二部图）
- ❌ P3（Alpha191 日线子集、SUE/PEAD）

## 六、Sprint 3 验收条目（2026-09-17 · 闭环合拢）

> 依据 `docs/Sprint3-闭环合拢修复计划-2026-09-17.md`（§0 固化决策 D1~D4），
> 修复复审报告 `docs/策略库审查报告-复审-2026-09-16.md` 全部 TOP 5 + 快修三件套。
> 执行环境 `.venv/bin/python3`；未切生产 profile、未提交 commit（§9 划线遵守）。

### 快修三件套（§1）

| # | 问题 | 修复 |
|---|---|---|
| K1 | earnings 关键词子串碰撞："净利润同比下滑"命中正面词"净利润同比"（pos=1/neg=1 假象） | `signals/earnings.py` 关键词表重构：POS 移除"净利润同比"换"净利润同比增长/预增"，NEG 加"净利润同比下滑/下降"；测试断言 `_scan_text("净利润同比下滑30%")` → pos=0 且 neg≥1 |
| K2 | premarket signal_eval 步骤重号（两个 6.7） | 改为 6.5 动态池 → 6.6 业绩预告 → 6.7 市场宽度 → 6.8 signal_eval |
| K3 | 测试与生产共用 `logs/signal_eval/`，factor_crowding.json 与 latest.json 已被测试污染 | `review/signal_eval._signal_eval_dir()` / `signals.signals._factor_crowding_path()` 优先读环境变量 `AGSICKLE_SIGNAL_EVAL_DIR`；`ai/bundle.py` 读取侧同步；相关测试全部沙箱化（try/finally 恢复）；删除污染文件并重跑 signal_eval 恢复真实 latest.json。验收：跑测试前后生产目录 mtime 零变化 |

### Fix-1 · breadth_composite 真 z-score + 5 指标补齐（复审 TOP 5 #2）

- `data/breadth.py` 新增 `_rolling_zscore`（近 250 日滚动 z，std=0 或样本 <60 → None，冷启动 60~249 返回实际窗口数）与 `_composite_from_z`（加权 z：0.30×z(涨停) − 0.30×z(跌停) + 0.20×z(涨跌比) + 0.20×z(新高新低)；任一缺失权重重归一化；全缺 → None）
- 封板率真实计算：em 源加 `stock_zt_pool_zbgc_em` 炸板池 → 封板率 = 涨停/(涨停+炸板)；失败 → None（不再硬编码 1.0，tx/sina 同为 None）
- `new_high_minus_new_low` 从 daily_bar 全表扫：60 日窗口创 60 日新高家数 − 新低家数（qfq 优先）
- 冷启动标注：`z_window=<N>` 记入 source 字段（如 `em|z_window=101`）
- 极端避险档（composite < −2 → cap 0.1）由死代码变为可触发且冷启动安全

### Fix-2 · ETF 升档 min-after override + pipeline 接入 + config 段（复审 TOP 5 #4）

- `_compute_etf_tier_shift` 改为只出方向信号 `{"direction": "up"|"down"|None}`；升/降档在 `compute_regime` 的 `cap_final = min(caps)` **之后**施加：up → `max(cap_final, cap_half)`（唯一允许往上提的路径，避险升半配），down → `min(cap_final, cap_half)`；`detail["etf_share"]` 记 direction + before/after cap
- premarket 步骤 3.5 接入 `refresh_bond_etf()`（`fetch_bond_yield` + `fetch_etf_share`，失败各自降级）——修复两表数据"接了表不接管线"的永陈旧问题
- config.json 新增 `bond_yield` / `etf_share` / `breadth` 三段（键名与 `_BOND_DEFAULTS`/`_ETF_DEFAULTS`/`BREADTH_DEFAULTS` 对齐，代码缺省兜底）

### Fix-3 · 规则 20 权重降级 + 退出滞回（复审 TOP 5 #3，D2）

- 权重降级：`_score_cross_section` 在 `state=="active"` 时以 `FC_DEGRADED_WEIGHTS=(0.20,0.20,0.15)` 打分（重归一化改选股排序），`score_parts.crowding_downgraded=true` 审计注记；cooling/off 正常权重
- 滞回状态机 `_crowding_next_state`（纯函数）：off→active（μ<0.005 且 σ>0.02，记 active_since）；active→cooling（μ≥0.015，计数 1）；cooling→active（μ 再破 0.005 清零回迁）；cooling 计满 5 次 compute_all → off；滞回带 [0.005, 0.015) 不翻转
- `factor_crowding.json` 新增 `state`/`active_since`/`cooling_count`；`crowded = state in ("active","cooling")` 兼容规则 20 读法；计算不可用保留旧 state（熔断不因一次失败意外解除）
- state 迁移写 `risk_event(rule='factor_crowding_state')`，同日同迁移去重
- 顺手修：`compute_all` 此前在 `c.close()` **之后**调 `_write_factor_crowding(c)`，conn=None（生产）路径每次必抛错——落盘从未成功过；已调序

### Fix-5 · v2 profile + earnings 硬约束（复审 TOP 5 #5，D3）

- `PROFILES` 加 `reversal_lowvol_v2`；五因子权重 `W_REV_V2..W_MAX_V2 = 0.30/0.20/0.15/0.20/0.15`；`score_reversal_lowvol_v2_xs` 五因子 `_rank01` 加权、缺因子重归一化（复刻 v1 模式）
- `_score_cross_section(rows, prof=None)` 双口径分支（v2 parts 含 ivol_rank/lowmax_rank）；`_factor_from_bars` 截面判断改 `not in ("reversal_lowvol","reversal_lowvol_v2")`
- signal 表加 `profile` 列：新库主键 `(code, as_of, profile)`；老库走 `_migrate_signal_profile` 重建迁移（旧行全部归 `reversal_lowvol`，53066 行无损迁移实测通过）；`signal_eval._signal_frame` 按 `config.signals.profile` 过滤读取
- backfill 加 `--profile` 参数（v1/v2 双 profile 并存回填互不覆盖）；backtest 加 v2 分支（`_ivol_max_panels` 面板化 IVOL/MAX），三 profile 对比写 `logs/backtest_result.json`
- `profile_verdict` 备选推导改显式枚举 `OTHER_PROFILES`（二元 if/else 在三 profile 下会错）；备选多于一票时 B/C 各取较优者对比，`alt_best`/`other_profiles` 落输出
- `ai/bundle.py::_signals_profile` 白名单加 v2
- earnings 硬约束：`check_blacklist` 查 news_earnings 近 3 日净分，`net ≤ −2` → 拦入黑名单（原因"业绩预告负面（net=-X）"）；`ai/decide.validate` 加可选 `earnings_events`：`net ≥ +2` → `confidence = min(1.0, confidence + 0.1)`，`load_and_save` 从 bundle `earnings_events_latest` 传入
- 实测产物（2026-09-17，回测窗口随日线自然滚动）：v1 ann=−15.72%/MDD −41.17%；v2 ann=+2.33%/MDD −25.10%；momentum ann=36.48%/MDD −59.04%——v2 回撤显著优于 v1，**是否切生产 profile 由用户人工验证后决定**（config.json `signals.profile` 保持 `reversal_lowvol` 未动）
- 生产 signal 表：v1 53066 行 + v2 55254 行并存（188 行差 = 09-16 新加 4 票无 v1 历史段，v2 全史回填所致）

### Fix-6 · P0-1 投票门槛放宽 + IVOL intercept（D4）

- `profile_verdict`：`votes_switch ≥ 1` → verdict=switch（原 2/2 全票逻辑删除）；输出加 `confidence`（2/2→high、1/2→low，low 提示人工复核权重）；红线 −30% override 仍优先于 switch；B/C 全弃权 → hold 不变
- `signals/factors.py::ivol` 回归带截距：`beta, alpha = np.polyfit(mr, sr, 1)`，`resid = sr − α − β·mr`（剔除个股相对基准的系统性日均漂移；注意 polyfit 返回 [斜率, 截距] 的顺序）
- `docs/决策策略与工作流.md` §7 同步 v1.5

### Fix-4 · 规则 21 主动扫描 + confirm 优先超时兜底（复审 TOP 5 #1，D1）

- 新表 `limit_halt_stuck(code PK, first_stuck_date, stuck_days, last_attempt)`；`decision` 表加 `emergency_scan` 列（DDL + `_MIGRATIONS`）
- `risk/engine.py` 抽公共判定 `_limit_halt_condition(code, ctx, cfg) -> (hit, info)`（跌停价/封单比/浮亏三条件），规则 21 与扫描器共用
- 新建 `signals/limit_halt.py`：
  - `scan_positions`：对 position 表逐票判定（avail_shares=0 的 T+1 票跳过），命中生成 emergency sell decision（price=跌停价、shares=avail_shares、`emergency_scan=True`）
  - `run_postclose_scan`（postclose 步骤 3.5）：扫描 → `runner.propose` 入库（**run_date=次日**——过 confirm 跨日闸门）→ macOS 通知；同票同 run_date 幂等
  - `run_intraday_scan`（intraday_check 14:50）：stuck 计数（首日 insert/后续 +1，不再命中即解除删除）；5 日 → `risk_event('limit_halt_stuck')` + 通知；同日 ≥3 只 → `apply_kill_switch`（既有人工 resume 链）
  - `premarket_failsafe`（premarket 步骤 0.5，数据抓取之前）：查 run_date=今日 且 status=approved 的应急单；09:14 前仅通知"N 笔待确认"；**09:14+ → `runner.confirm(confirmed_by="emergency_timeout_failsafe")` 自动执行**（confirm 内置风控复跑与执行价二次校验）
- 审计链：`emergency_scan`（扫描生成）→ `emergency_timeout_failsafe`（超时自动）/真人（人工 confirm）→ trade 表完整归因
- 配套风控豁免（均为 emergency_scan 单专用，正常单不受影响）：
  - 规则 4 交易时段：盘后 propose / 盘前 09:14 confirm 均不在连续竞价时段，按普通单会拒——应急单入库/兜底确认放行（执行价二次校验保留）
  - 规则 18 流动性：连续跌停日成交额萎缩，全仓逃命单几乎必然超 1% 参与率——跌停价排队单豁免
  - `_decision_from_row` 回读时带回 `emergency_scan` 等标志（此前只回 order，豁免判定会失效）
  - 规则 21 对 `emergency_scan` 单不再设 `skip_gate` 直写（D1 是 confirm 优先，扫描单必须停在 pending）
- paper 层流动性防线未豁免：真实生产中极端萎缩票被拒 → `execution_failed` event + 通知人工介入（出路保留）

### 与复审 TOP 5 的对照

| 复审指控 | 修复位置 | 测试证据 |
|---|---|---|
| #1 规则 21 只被动响应，LLM 不主动卖则连续跌停死锁；无 stuck 计数与 kill 联动 | `signals/limit_halt.py`（扫描器/三入口）+ `engine._limit_halt_condition` + 步骤 0.5 兜底 + 表/列迁移 | tests/test_limit_halt.py 7/7：三条件命中与不命中、propose 入库+幂等（run_date=次日、emergency_scan=1）、stuck 递增+5 日 event 去重、3 只 stuck → kill_switch=1、09:14+ 自动执行且 `trade.confirmed_by="emergency_timeout_failsafe"`、09:00 仅通知、无单空转 |
| #2 breadth_composite 原始值算数平均，threshold=−2 永不触发（极端避险死代码）；seal_rate 硬编码 1.0；新高新低恒 None | `data/breadth.py` 真 z-score + 权重重归一化 + 炸板池封板率 + daily_bar 全表扫新高新低 + 冷启动保护 | tests/test_breadth.py 13/13：z 与 numpy 手算一致（1e-9）、std=0/<60 样本 → None、58 样本 → composite=None、100 样本 → 实际窗口+权重重归一化手算一致+source 标注 `em\|z_window=101`、新高新低计数（+1−1+0+跳过+1）、封板率 3/(3+1)=0.75、炸板池失败 → None |
| #3 规则 20 权重从不降级；crowded 无滞回直接翻转 | `_score_cross_section` 降权 + `_crowding_next_state` 滞回 + risk_event 留痕 | tests/test_signals.py：滞回状态机（触发/滞回带不翻转/清零回迁/计满 5 次退出/off 带不触发）、降权后低波票上位+crowding_downgraded 注记+cooling 不降权、迁移 event 同日去重、计算不可用保留旧 state |
| #4 ETF 升档在 min(caps) 下永被 0.2 覆盖（死代码）；国债/ETF 表不接管线永陈旧；阈值硬编码 | `compute_regime` min-after override + premarket 步骤 3.5 + config 三段 | tests/test_regime.py 18/18：升档（避险 0.2+ETF+3% → 0.5、cap_before/after 留痕）、降档（国债 0.64+ETF−3% → 0.5）、无信号不干预（0.64 保持）；tests/test_macro.py +1（refresh_bond_etf 调两路且单路失败不阻断）；tests/test_breadth.py breadth 阈值 config 化回归 |
| #5 v2 打分零落地（IVOL/MAX 展示字段）；earnings net≤−2 无硬拦截；confidence +0.1 无代码 | `signals.py` v2 五因子 + signal 表 profile 列 + backfill/backtest 双口径 + `check_blacklist` 硬拦截 + `decide.validate` confidence 加成 | tests/test_signals.py：v2 排序预期、缺因子重归一化、v1/v2 行并存互不覆盖+按 profile 过滤读取；tests/test_risk_engine.py：net=−3 拦/net=0 过/过期不拦；tests/test_ai_pipeline.py：+0.1/封顶 1.0/净分不足不加成；tests/test_review.py：三 profile 备选枚举+较优对比 |

### 回归验证（2026-09-17 实测，19 文件 271 用例全绿；Sprint 3 前基线 242）

| 测试文件 | 结果 | Sprint 3 增量 |
|---|---|---|
| tests/test_ai_pipeline.py | 19/19 | +1（earnings confidence 加成/封顶） |
| tests/test_audit.py | 5/5 | — |
| tests/test_breadth.py | 13/13 | +6（z-score 手算/std=0/冷启动/实际窗口重归一化/新高新低/封板率×2） |
| tests/test_execution.py | 28/28 | — |
| tests/test_fetcher_breaker.py | 3/3 | — |
| tests/test_limit_halt.py | 7/7 | 新建（扫描/幂等/stuck/kill/兜底×3） |
| tests/test_macro.py | 9/9 | +1（refresh_bond_etf 冒烟）；装饰器 `__test__` 修 pytest 收集 |
| tests/test_market.py | 7/7 | — |
| tests/test_movers_hot.py | 7/7 | — |
| tests/test_news_macro.py | 7/7 | — |
| tests/test_pipeline.py | 3/3 | — |
| tests/test_pipeline_full.py | 6/6 | teardown_module 恢复 env（防同进程泄漏 AGSICKLE_DB） |
| tests/test_quotes.py | 9/9 | — |
| tests/test_regime.py | 18/18 | +3（ETF 升档/降档/不干预，min-after override）；ETF 方向语义改造 ×2 |
| tests/test_repo.py | 11/11 | — |
| tests/test_review.py | 22/22 | +1（三 profile 备选较优对比）；1/2 票 case 改 switch+low（v1.5） |
| tests/test_risk_engine.py | 45/45 | +1（blacklist earnings 硬拦截） |
| tests/test_signals.py | 47/47 | +4（滞回状态机/降权/迁移 event/保留 state）+3（v2 打分/缺因子/profile 隔离）+1（ivol intercept）；K1 子串碰撞 ×1 |
| tests/test_webapp.py | 5/5 | — |

### 端到端冒烟（2026-09-17 凌晨实测）

1. `compute_regime`（生产库只读）：七路并联全部工作——RSRS z=−0.161、二八皆弱→避险档 cap=0.2、国债 mult=1.0、ETF direction=None（表空 fail-open）、breadth composite=None（冷启动保护生效，不误触极端避险）
2. premarket 干跑 exit=0：步骤 0.5（兜底，无 pending 空转）、3.5（bond/ETF 刷新——债券成功入库 10Y 1.6858%；ETF 凌晨源无数据降级继续）、6.6（业绩预告 2 票命中）、6.7（市场宽度 em 源真实落库：涨停 89/封板率 0.89/跌停 4/新高新低 −82/composite=None 冷启动）、6.8（signal_eval 落盘 OK）
3. `review.signal_eval` 生产 latest.json：三 profile verdict 正常——B/C 取较优备选（momentum/v2）、2/2 票投切换但红线 override（C_cur=−41.17% < −30%）→ hold，语义正确
4. signal 表迁移：53066 行全数无损迁至 `profile='reversal_lowvol'`；v2 回填 55254 行并存

### 遗留与划线（§9 重申）

- ❌ 切生产 profile 到 v2（v2 验证产物已就绪：ann/MDD 均优于 v1，由用户人工决定；改 `config.json → signals.profile` 即生效，mtime 热读不重启）
- ❌ §5.4 概念集中度大类聚合（Sprint 4）；❌ backtest_full.py 生产口径回测；❌ iVIX/期权 PCR 等 P2/P3
- ❌ 提交 git commit（等用户指示）
- 备忘：test_breadth.py / test_macro.py 的 `@test` 装饰器加了 `__test__=False`（此前被 pytest 误收集为 collection error）

---

## Sprint 3 复审第一梯队修复（P1-6 → P0-5 → P0-1，2026-09-17）

> 背景：`docs/Sprint3多agent审查报告-2026-09-17.md` 判定 Sprint 3「不通过」，独立复核（`docs/Sprint3审查报告-复核意见-2026-09-17.md`）确认 21 条指控 19 属实。按复核报告「先堵资金风险」顺序，本批修复 P1-6 / P0-5 / P0-1 三条——它们共同决定「会不会出现非授权自动卖出 / 止损出口被焊死 / 破线持仓被误判连续跌停」。

### 三条修复对照

| 复审指控 | 修复位置 | 测试证据 |
|---|---|---|
| **P1-6** skip_gate 直写路径已接通且无开关：规则21 对非 emergency_scan 的 LLM 跌停价卖单置 `skip_gate=True`，runner 无条件直写成交、绕过人工闸门，违背「绝不自动成交」总原则 | `config.json` execution 段新增 `emergency_direct_exec: false`；`execution/runner.py` propose 分支改为 `direct_exec = gate_off or (skip_gate and emergency_direct_exec)`——默认开关关时 skip_gate 单**仍走 pending 人工闸门**；`risk/engine.py` 规则21 warning 文案改为准确描述（不再谎称「自动成交」） | tests/test_execution.py 29/29：新增 `test_propose_skip_gate_defaults_to_pending_gate`（默认关→落 pending、0 成交、status=approved）+ 改写 `test_propose_skip_gate_direct_exec_requires_switch`（显式开→直写、confirmed_by=emergency_rule21） |
| **P0-5** 业绩预告黑名单连 SELL 一起拦：持仓票出负面预告后 3 日内 LLM 止损卖单（含规则21 应急单）在 validate 与规则1 两层被拒，连续跌停+负面预告恰是规则21 典型场景，出口被自己的黑名单焊死 | `risk/blacklist.py` 新增 `EARNINGS_BLOCK_PREFIX` 常量 + `is_earnings_only()`/`split_blacklist_reason()`；`risk/engine.py::rule_blacklist` 对 `action=='sell'` 且拦截**仅**由业绩预告负面构成时豁免放行（含 ST 等其他理由仍拦）；`ai/decide.py::validate` 同口径豁免 | tests/test_risk_engine.py 50/50：新增 5 例（is_earnings_only 各输入、sell+仅业绩预告→豁免、sell+ST→仍拦、sell+混合→仍拦、buy+业绩预告→仍拦）；tests/test_ai_pipeline.py 20/20：新增 `test_validate_sell_exempts_earnings_only_blacklist` |
| **P0-1** 应急扫描缺「当日跌停」条件①：`scan_positions` 命中 `_limit_halt_condition` 后直接造 `order.price=down`，共用函数注释明示「由调用方比对 order.price==down」但扫描器自造该价使比对恒真，三条件实际只剩「浮亏≥止损线」→ 任意破线持仓（即使当日上涨远离跌停）被误判连续跌停应急单，进而误触 stuck 计数与全账户 kill | `signals/limit_halt.py::scan_positions` 在 hit 判定后补条件①：`lp = latest_prices.get(code); if lp is None or lp > down*(1+0.005): continue`（现价缺失或高于跌停价 0.5% 以上→未跌停→跳过） | tests/test_limit_halt.py 9/9：新增 `test_scan_miss_when_not_limit_down`（昨收100/今收103/成本130 浮亏20.8%破线但未跌停→不生成）+ `test_scan_miss_when_latest_price_missing`（现价缺失→保守跳过）；原 7 例（含真跌停命中、propose 幂等、stuck、kill、09:14 兜底）保持通过 |

### 回归中顺手修掉的 2 个既有 bug（非本批三条引入，但阻断全绿）

| 既有 bug | 根因 | 修复 |
|---|---|---|
| `test_compute_all_synthetic_db` 失败（len=1≠2） | Sprint3 给 `compute_all` 加 `watchlist_only=True` 默认，按 `config.watchlist_core`（51 只核心）过滤；测试合成码 000001 不在 watchlist_core 内被滤掉。测试意图是验证 compute_all 机器本身，与 watchlist 过滤无关 | tests/test_signals.py：该测试显式传 `compute_all(conn=conn, watchlist_only=False)` 走全 universe 路径 |
| `test_compute_signal_writes_ivol_max_fields` 在空库/隔离环境下失败（max_ret_5_20d=None） | Sprint3 P1-2 落地时把 `max_ret_5_20d`（个股自身 top-5 日收益，**不依赖基准**）误 gated 在 `if bench_close is not None` 分支内；HS300 缺失（空临时库）时被连带置 None。生产库因有 HS300 而侥幸通过，暴露测试对生产库状态的隐式依赖 | signals/signals.py：`max_ret_5_20d` 移出 bench_close 分支无条件计算（`max_ret_bali` 对短序列安全返回 None）；`ivol_20d` 仍留在分支内（确需基准做 CAPM 残差） |

### 回归验证（2026-09-17 实测）

- **pytest 全量**：`AGSICKLE_DB=/tmp/... pytest tests/ -q` → **276 passed, 0 failed**（3.58s，生产库隔离保护）
- **官方 runner**：`tests/run_all.py`（subprocess 直接执行、生产库）→ **16/16 文件通过**（9.0s），与改前基线一致
- 用例增量：test_execution 28→29、test_risk_engine 45→50、test_ai_pipeline 19→20、test_limit_halt 7→9（+9 新例，2 既有 bug 修复不增减用例数）
- 注：`run_all.py` 不带 `AGSICKLE_DB` 时经 **P0-4 既有泄漏**（engine.check 内部 get_conn 直写生产库）会向 `risk_event` 追加测试残留（本次 +3 条 `limit_halt_emergency`，detail 为测试 seed「600519…卖价=1341.00」）；pytest 全程用 `/tmp` 隔离库，未污染生产

### 遗留（不在本批 P1-6/P0-5/P0-1 范围）

- **P0-4**（engine.check 内部 get_conn 直写生产库）未修：生产 `risk_event` 今日累计 ~93 条测试残留 `limit_halt_emergency` + 1 条 `factor_crowding_active`，需按复核报告 §4 清理并落实 DB 隔离（RiskContext 加 conn 字段或测试套件统一设 AGSICKLE_DB）
- 复核报告其余 P0（P0-2 confirm 跨日断裂、P0-3 failsafe 无调度+catchup launchd 瘫痪、P0-6 腾讯字段错位、P0-7 涨跌家数接口错、P0-8 breadth 盘前采集、P0-9 UI 零展示）与 P1（P1-1 关键词嵌套、P1-2 stuck 计数、P1-3 signal profile 四处漏过滤、P1-4 IVOL 对齐、P1-5 backfill 拥挤 state、P1-7~P1-12）待后续批次
- ❌ 未提交 git commit（等用户指示）

---

## 审查补丁批（Fix A–F，2026-09-17）

> 背景：对上一批（P1-6/P0-5/P0-1）做独立三路代码审查 + 生产库核查后，确认三条修复**全部正确落地**，同时发现 6 个新问题（含 1 项高危：skip_gate 可从决策输入注入）。本批按「先堵资金风险」原则修复。
> 用户决策：① 补丁批优先于下一梯队；② 09:14 兜底自动执行**跟随总开关**（`emergency_direct_exec=false` 时只通知不执行），kill 清仓保留为独立风控防线、`manual_gate` 语义不动；③ 生产库残留**备份后清理**。

### Fix A · 堵 skip_gate/confirmed_by 注入面【高】

| 问题 | 修复位置 |
|---|---|
| `ai/decide.py` 落库的 input_snapshot 存的是 LLM **原始 JSON**，`runner._decision_from_row` 会从中恢复 `skip_gate/confirmed_by` → 一旦 `emergency_direct_exec=true`，任何自带 `skip_gate:true` 的决策（**含 buy**、无需规则21 条件成立）都直写成交，`confirmed_by` 可伪造落审计。原 P1-6 两个测试恰以"手工注入 skip_gate"为正规路径 | ① `execution/runner.py::_decision_from_row` 恢复白名单从 4 键减为 `emergency_scan/emergency_pending_skip`（剥离 skip_gate/confirmed_by 并 log.warning）；② `propose()` 入口对传入决策再剥一次（`decision.pop`）；③ `load_decision_file`（--file 路径）同样剥离；④ 直写分支加硬约束 `action == "sell"`（buy 即使带 skip_gate 也绝不直写）；⑤ 合法应急单的 skip_gate 由规则21 在 propose 重跑 check 时重新置位（已核实），confirm 复检豁免只依赖 emergency_scan/emergency_pending_skip，不受影响 |

### Fix B · 盘中 stuck 计数改用实时快照【中高】

| 问题 | 修复位置 |
|---|---|
| `scan_positions` 的 `latest_price(live=False)` + intraday_check 不写 daily_bar → 14:50 的条件①实际比对"昨收 vs **前日**跌停价"＝检测**昨日**跌停：昨日跌停今日反弹仍计数，3 只即假触发 72h kill（停机期规则5 连止损/应急卖单都拒） | `pipeline/intraday_check.py` 把已拉取的实时快照（price+prev_close）传入 `run_intraday_scan(conn, now, latest_prices, prev_close)`；`signals/limit_halt.py::run_intraday_scan` 新增两参数并转发 ctx_inputs，**显式给现价时缺价票不再回退日线收盘补齐**（防"缺实时价伪装成昨收价"）；快照整体为空（行情失败）时本轮不计数也**不清除**既有 stuck；未传快照（回放/测试）走原日线口径并 log 警告 |

### Fix C · P0-4 泄漏修复 + 生产库残留清理【中】

| 问题 | 修复位置 |
|---|---|
| `risk/engine.py` 两处 `get_conn()` 直写生产库（:765 规则21事件、:668 factor_crowding），测试运行即污染生产 `risk_event` | `Verdict` 新增 `events` 字段；engine 改为挂 `v.events`（含 `once_today_prefix` 同日去重）；新增 `risk.engine.flush_events(conn, v, decision_id)` 由持有 conn 的调用方落库（runner.propose/confirm 各调一次，事件带 decision_id 可归因）；engine 内不再出现 get_conn |
| 生产残留 | **已清理**：先 `cp data/market.db data/market.db.bak-20260917`（200M）备份 → `DELETE FROM risk_event WHERE rule='limit_halt_emergency' AND decision_id IS NULL`（142 条测试 seed，全部 decision_id 为 NULL）+ 删 2 条 `factor_crowding_active` 合成值行（detail 含 μ=0.001）→ 前后 `SELECT rule, COUNT(*)` 对账：142→0、2→0，7 条真实事件（watchdog_stale/risk_check/profile_verdict_red_line/risk_check_reconfirm/factor_crowding_state）全数保留，总数 155→13 |

> ⚠️ 勘误：三审报告附录的清理 SQL 用列名 `kind`，实际 `risk_event` 列为 **`rule`**（schema: id/ts/rule/detail/decision_id），执行时须修正。

### Fix D · 测试回归盲区【中】

| 问题 | 修复位置 |
|---|---|
| `run_all.py` 的 FILES 不含 test_limit_halt/test_breadth/test_macro → P0-1 新测试与 breadth/macro 用例不在官方回归口径内 | FILES 补入 3 个文件（16→19），实测三者可独立直跑且不写生产库 |
| `test_risk_engine.py` 的 Fix-5 测试位于 `__main__` 块 `sys.exit()` **之后**，subprocess 直跑永不执行 | 前移至 `__main__` 之前（pytest 与直跑双口径可见），文件用例 50→51 |
| 测试污染生产 signal_eval（新发现，与 P0-4 同类）：`test_pipeline_full.py::_restore_env()` 在 teardown 清掉"导入时不存在"的 `AGSICKLE_*` 变量 → 连带清掉其它文件设置的 `AGSICKLE_SIGNAL_EVAL_DIR` 沙箱 → 规则20/打分降权读**生产** `factor_crowding.json`（2026-09-17 起真实 crowded=true），12 个风控用例 + 2 个信号用例批量失败；且 compute_all 会把合成拥挤状态**写回生产目录** | ① 新增 `tests/conftest.py`：autouse fixture 逐用例注入全新沙箱目录（防读污染+写污染）；② `tests/run_all.py` 为每个 subprocess 注入沙箱 env；③ test_pipeline/test_pipeline_full 模块级 setdefault 沙箱（覆盖直跑）；④ test_signals 的拥挤降权基线显式打桩 `read_factor_crowding`（生产已真实激活拥挤，不打桩则基线即降权） |

### Fix E · 三个小加固【低】

| 问题 | 修复位置 |
|---|---|
| `enforce_stuck_rules` 在 `now=None` 且 ≥3 只 stuck 时 `now + timedelta` TypeError | 函数开头 `now = now or datetime.now()` |
| 跌停判定容差 `down*0.005` 在 down<2 元时不足 1 分钱，覆盖不了数据源 1 分精度误差 | 容差改 `max(down * 0.005, 0.01)`（保底一分钱） |
| `rule_blacklist` 的 sell 豁免用原始 action 比较（`check()` 主流程用归一化 action），口径不齐 | 改用 `str(action).strip().lower()`（非常规输入 fail-closed 走拦截） |

### Fix F · 09:14 应急兜底跟随总开关（用户决策）

| 问题 | 修复位置 |
|---|---|
| `premarket_failsafe` 09:14 自动 confirm 不受 `emergency_direct_exec` 管辖——"总开关"名不副实（开关关着仍有自动成交路径） | `signals/limit_halt.py::premarket_failsafe` 读 `execution.emergency_direct_exec`：false（默认）时 09:14+ 只通知"需人工 confirm/reject"不执行；true 时维持原自动执行。`run_postclose_scan` 与规则21 的 warning/通知文案同步改为如实反映开关状态。kill 清仓（14:50 回撤触线）按用户决策**保留为独立风控防线**、`manual_gate=false` 直写语义不动 |

### 补丁批回归验证（2026-09-17 实测）

- **pytest 全量**：`AGSICKLE_DB=/tmp/... pytest tests/ -q` → **281 passed, 0 failed**（3.26s）
- **官方 runner**：`tests/run_all.py` → **19/19 文件通过**（9.9s，含新增 3 文件），生产库生产文件均未被写（risk_event 保持 13 条、factor_crowding.json mtime 仍 15:43）
- 用例增量：test_execution 29→30（注入面回归 1 例）、test_limit_halt 9→13（Fix B 3 例 + Fix F 1 例）、test_risk_engine 50→51（死测试前移）
- 生产库清理对账：155→13 条（142+2 测试残留删除，7 条真实事件保留），备份 `data/market.db.bak-20260917`

### 本批遗留待办（已记录，建议下批或并入对应项）

- **ST 无 5% 档**：`common/market.py::limit_pct` 无 ST 分支（audit 既定"不区分 ST"），规则21 对 ST 票从"错价应急单"变为**静默跳过**（真跌停 0.95pc > down×1.005）——属既有限制被 P0-1 放大，需单独决策是否识别 ST
- **条件②封单比生产恒跳过**：`live_quotes` 两个生产入口都不传，实际只有"价格≈跌停 + 浮亏破线"两条件
- **hold/watch 黑名单口径不一致**：engine 对 hold/watch 完全不查黑名单，decide.validate 连"仅业绩预告"也拦
- **backfill 与 compute_all 的 universe 分叉**：`backfill_history` 全宇宙 vs `compute_all` watchlist_only=True（51 只核心），signal 表历史/新日期广度不一致，signal_eval 的 pooled IC 口径漂移（docstring"完全同口径"已失真）
- 复核报告其余项：P0-2 confirm 跨日断裂、P0-3 failsafe 无调度 + catchup launchd 瘫痪、P0-6 腾讯字段错位、P0-7/P0-8/P0-9 与 P1 系列
- ❌ 未提交 git commit（等用户指示）
