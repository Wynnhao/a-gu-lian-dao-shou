# 施工方案：全量打包批（对冲配对尾盘口径 R 批 + 缠论 LLM 证据版 + X 批次解冻 + 工程清债）

> 2026-09-20 · 前置为当日四项用户裁决：对冲配对走 **①+② 串行打包**（先尾盘口径预注册
> 研究批，无论 pass/fail 都落 LLM 证据字段）、缠论线 **LLM 证据版立项**（R3 机械线归档
> 不复活）、**X 批次解冻独立立项**、计划范围 **全量打包**（研究 + P2×21 + qfq + 核销 +
> 录制器体检）。
> 纪律照 R1/M1/R3 惯例：判据先定后跑、gate fail 即归档、禁止调参/换口径重跑、
> 再评估须另立新预注册批次。

## 1. 背景数字与裁决依据（均有留档，来源见括号）

| 线 | 关键事实 |
|---|---|
| 对冲配对 | 同日跷跷板真实：防御股跌当日对冲股 84%/79% 在涨（基准 52%/53%）；但 T+1 口径 edge 崩塌（25 对仅 4 对超基准 +4pp）→ 尾盘 14:50 是唯一可交易口径；250 日 ρ<−0.3 仅 5 对（防御↔科技）、前后半窗 20/20 同负（关系稳定）；历史无分钟数据，录制器 2026-09-21 起积累（pair-hedge 记忆 2026-09-19） |
| 缠论证据版 | R3 Gate1 PASS（clean 231/月桶中位 5）但 Gate2 重画率 61.46% FAIL——机械信号因果不可实现；结构引擎 chanlib 四模块资产验收合格；LLM 证据版的消费方式（软证据、不做机械 gate）天然规避机械信号的可交易性要求，但须先在定义层重钉"首发确认即锁定"因果口径（R3 方案 §6.3） |
| X 批次 | R1 概念轮动 FAIL 归档（白名单 1/20 月非空、2 信号与动量基线重合年化超额 +0.00%；实证 edge 属近 60 日行情特异+事后可见）；X 冻结原文："尾盘盘中口径、申万行业粒度"（概念轮动方案 L146）；解冻后按单变量原则拆 X1/X2 两轴 |
| 工程清债 | Sprint4 完工审查 4 域 pass-with-fixes 无 P0，P2×21 留档（风控执行 ①~⑥ / 数据 ⑦~⑫ / LLM 上下文 ⑬~⑮ / 选型 ⑯~⑳ / 集成 ㉑），审查建议优先 ⑦⑧⑨（数据写路径）与 ⑬（降级日愈合）；W-A2 剩余范围 = 规则4/18/3 豁免 + 批次A 测试（修复计划 L52 核验节） |
| qfq 数据层 | core 51 中 33 票、129 处 \|ret_qfq−ret_raw\|>1pp——**多数预期为真实除权日被 qfq 正确吸收**（002594 2025-07-29 raw −66.94% vs qfq +0.37% 即真经济收益）；真实缺口是无 open_qfq 列（R3 加法构造在 002594/300308/300274 三送转票原理性不成立）与除权日缺元数据（R3 批次 0 报告） |
| 录制器 | C-ARC T6 已接线 crontab（`*/5 9-15 * * 1-5`，交易日+竞价双 gate）；首个完整交易周自 09-21（周一）开始；Sprint4 遗留行动项 1 = 周一首 tick 确认 + DoD③ 两日生产验证 |

## 2. 范围与非目标

**做**：qfq 数据层审计与补列（open_qfq 等比列 + 除权日元数据 + 审计驱动重刷）；
对冲配对尾盘口径预注册研究批（gate 三件套 + 稳定配对清单产出）；缠论 LLM 证据版
（确认口径因果化重钉 + 证据标签流 + 强制披露，无 pass/fail gate）；X 批次解冻重测
（X1 尾盘口径 / X2 行业粒度，单变量原则）；P2×21 清债（分域三 agent）；W-A2①③ 与
遗留行动项核销；bundle 证据字段合入（对冲② + 缠论标签，字段只增）；结题 + 录制器
首周体检（09-25 盘后条件触发）。

**不做（本批红线）**：R2/M2 不开工；R3 机械线不复活、不调参重跑；策略库 IVOL+MAX(5)
不在本批（另批排队）；不做实盘/真实资金任何接入；不扫参数（对冲批阈值 −1.0%/上调
30%/持有 1 日、X 批阈值与白名单规则全部逐字冻结）；全库 qfq 不做无据重刷（重刷仅由
审计结果驱动）；不改 config.json、不动既有 crontab 条目。

## 3. 预注册口径（批次 0 commit 后不可变；各研究线原文须完整抄入对应脚本 docstring）

### 3.1 批次 0 · qfq 数据层（唯一授权的生产数据变更批）

**先审计后修复，重刷仅由审计结果驱动。**

- 审计 A1（只读）：全库畸变日清单（|ret_qfq − ret_raw| > 1pp，ret_raw=close.pct_change，
  ret_qfq=close_qfq.pct_change）+ 除权除息事件清单（akshare em 除权数据在线取，只进内存
  与报告，不入库）。
- **Gate 0-A（畸变解释完备性）**：每个畸变日必须落在该票除权事件清单上（事件日或其后
  首个交易日，容忍除权净额归零导致的边界 ±1 日）；存在无解释畸变日的票 → 该票以 em 单源
  重拉全史 qfq（事务内 UPDATE 该票 close_qfq/high_qfq/low_qfq）后复检；复检仍无解释 →
  **停下上报，不得硬修**。
- **Gate 0-B（open_qfq 等比补列）**：`open_qfq ≡ open × (close_qfq / close)`，与现行
  high_qfq/low_qfq 同等比口径（fetcher.py L8-10 语义），全库 ALTER TABLE 增列 + 回填；
  fetcher 增量写路径同步该式（增量一致性）；逐日校验 low_qfq ≤ open_qfq ≤ high_qfq
  违例 = 0（浮点容差 1e-6 相对）。
- **Gate 0-C（双源对账）**：抽样 ≥50 票（含全部畸变重灾票）与 tx_qfq 逐日收益对账：
  收益差中位 ≤ 0.05%，|差|>1% 的点逐票 ≤ 2 处（源间口径差容忍；超限票单独列报告）。
- **Gate 0-D（无除权区间不变量）**：无除权事件的相邻交易日，qfq 收益 = raw 收益
  （相对容差 1e-9）逐日成立。
- 生产安全红线：执行前备份 market.db（`market.db.bak-20260920` 惯例）；全部写操作单事务
  且仅限 daily_bar 的 close_qfq/high_qfq/low_qfq/open_qfq 列族（**绝不触其他任何表**）；
  写后跑全库数据体检（audit 路径）+ 回滚预案写入批次报告；除权事件清单与审计报告落
  `docs/批次0-qfq数据层报告-2026-09-20.md`。
- 附带义务：tests/run_all.py FILES 预注册本批全部新测试文件名：`test_qfq_rebuild.py`、
  `test_hedge_pair.py`、`test_chan_evidence.py`、`test_rotation_x.py`（文件由后续批次创建，
  run_all 跳过缺失，避免并行 Edit 冲突——R3 惯例）；周一（09-21）开盘后确认 crontab 首
  tick 真实执行（Sprint4 遗留行动项 1，仅确认与记录，不改 crontab）。

### 3.2 批次 1 · 对冲配对尾盘口径研究批（预注册 gate）

脚本 `signals/hedge_pair_research.py`（mode=ro、预注册 docstring、退出码 0=pass/1=fail）。

- **配对池**：config.json `watchlist_core`（51 只，可交易性约束；extended 观察票不得作为
  配对腿）。
- **配对白名单（月度重估，照 R1 月频红线）**：每月末用截至当日 250 日窗重估、次月全月生
  效；入选对需 ρ_250d < −0.30 且前后半窗（各 125 日）ρ 同为负 且 ρ_60d < 0。日收益用
  close_qfq。
- **触发（尾盘口径，全部主口径参数冻结）**：生效白名单内配对 (A,B)，A 当日收益 < −1.0%
  且 B 当日收益 > 0 → 当日尾盘执行：B 权重上调至 30%（底仓其余票等比缩）；**次日尾盘回落
  等权**（持有 1 日）。同对每 ISO 周最多触发 1 次；窗口重叠最新优先。
- **组合构造**：底仓 = 受控 momentum Top5 等权各 20%（每月末换持，照 R3 §3.3/§3.6-10 冻结
  细节；**表述红线：受控对照腿，非生产 momentum profile**）；B 不在底仓时买入（挤出现金或
  等比缩其余仓）；成本每边 0.15% + **尾盘乐观偏差扣减 0.3%/次调仓**（14:50 观测≈收盘价
  的乐观偏差预注册扣减；敏感性 0/0.3%/0.5% 三档只披露，主口径 0.3%）。
- **Gate（先定后跑，逐项过；fail 即归档）**：
  - Gate H0 分辨率：去重后触发 ≥60 且 触发月桶中位（含零月）≥3；不足 → 判"无分辨率"直接
    FAIL，不做超额归因。
  - Gate H1：扣成本年化超额（信号腿 − momentum 基线腿，算术差）≥ +5%。
  - Gate H2：2025H1 / 2025H2 / 2026 三段超额 ≥2 段为正（段内简单收益差）。
  - Gate H3：信号腿 MDD 劣于基线 ≤2pp。
- **诊断（非 gate，如实披露）**：分对贡献拆分；**T+1 执行对照组**（同信号改次日执行——
  预期超额消失，作为"跷跷板同日性"机制验证）；尾盘扣减三档敏感性；两种配对腿方向
  （A 跌买 B / B 跌买 A）检出数。
- **无论 gate 结果必须产出**：稳定配对清单生成函数（月度重估规则实现 + 当期清单），供
  批次 6 证据字段复用——**证据合入不依赖 gate**（用户裁决②）。

### 3.3 批次 2 · 缠论 LLM 证据版（无 pass/fail gate；口径重钉冻结 + 强制披露）

脚本 `signals/chan_evidence.py`（mode=ro，复用 chanlib/chan_causal/chan_data 资产）。

- **确认口径因果化重钉（本批冻结，替代 R3 机械信号语义）**：
  1. 标签流由 chan_causal 前缀重算逐日生成；**历史标签永不回改**（首发确认即锁定）。
  2. 三买状态标签：首发确认日锁定为"三买持仓态"，在因果流中出场条件触发（顶分型确认
     或 20 日先到，照 R3 §3.6-8 出场计数）后转为"三买结束"；此后同枢再确认按新事件记录。
  3. 结构上下文标签逐日更新但只增不改历史：当前笔方向、最近顶/底分型类型与距今交易日数、
     最近中枢 [ZD,ZG] 与现价相对位置（上/内/下）。
- **强制披露（结题如实回填，不设阈值 gate）**：①锁定三买标签 T+20 终态相容率（与
  chanlib 全史终态结构不矛盾的比例）；②标签日变更频率/票；③覆盖率（warmup 后有标签
  票数/50）；④与 R3 机械信号流（288 实发）的关系说明。
- **产出**：逐票逐日标签流（内存/临时产物，不入库）+ bundle 字段规格（每票一行结构摘要，
  token 上限照滞后票墙惯例，具体文案批次 6 定）+ 结题附 3 票样例供用户人工验收。
- 验收：单测绿（锁定语义/不回改/出场解锁/标签域）+ core 51 全池实跑统计产出。

### 3.4 批次 3 · X 批次解冻（X1/X2 两轴，单变量原则，gate 照抄 R1）

脚本 `signals/rotation_x.py`（mode=ro、预注册 docstring、退出码 0/1；**不改 rotation.py
冻结脚本**，白名单/组收益逻辑 import 复用）。

- **X1 尾盘口径轴**：R1 白名单规则（ρ_250d < −0.15 且前后半窗同负 且 ρ_60d < −0.20、月度
  重估）与触发阈值（A 组日收益 < −0.8%、同对每 ISO 周最多 1 次）**逐字不变**；唯一变更 =
  执行口径：触发**当日尾盘收盘买入 B 组、T+1 尾盘收盘回落**（原 R1 为 T+1..T+3 接力窗口）；
  成本换腿 0.1% + 尾盘扣减 0.3%（同 3.2 主口径）；T+2/T+3 持有变体只披露不作 gate。
- **X2 行业粒度轴**：白名单规则与执行口径（T+1..T+3 接力）**与 R1 逐字相同**；唯一变更 =
  组成员从概念组换为**申万一级行业**（映射经 akshare 在线取、只进内存、失败即阻断上报）；
  组日收益等权、组内 ≥6 只有数据才计（照 R1 MIN_MEMBERS）。
- **Gate（两轴各自独立判定，判据与 R1 rotation.py 三件套逐字相同）**：① 扣成本年化超额
  ≥ +5%；② 三段 ≥2 正；③ MDD 劣化 ≤2pp（均对照各自口径的 momentum 基线组）。fail 即归档
  该轴；禁止调参。
- 测试窗与 warmup 沿用 R1 口径（首个满足 250 日重估窗的月份起）。

### 3.5 批次 4 · P2×21 清债（三 agent 分域并行，每域一 commit）

范围 = Sprint4 完工审查报告 §P2 留档清单全量 21 项。验收原则（预注册）：

- 每项修复 = 行为修复 + 回归用例 + 不引入任何口径变化、不顺手重构；全部修复后 tests/
  全量绿。
- 分域归属：**P2-A** 风控执行 ①~⑥ + 集成 ㉑（risk/execution/deploy）；**P2-B** 数据
  ⑦~⑫（data/fetcher、pipeline 写路径；⑩ 为 audit --fix 操作项，执行后以 audit 输出为
  验收；⑫ 删除兼容条款须附负向用例）；**P2-C** LLM 上下文 ⑬~⑮ + 选型 ⑯~⑳（ai/ 与
  signals/backtest、tests）。
- 优先级尊重审查建议：⑦⑧⑨⑬ 首日完成；⑨ 的 fetch_log 冷却窗与 ⑬ 的 _mm_done 愈合分支
  按 审查报告 内建议方向实现，行为语义变化须在 commit 信息与 ADR 附记中写明。

### 3.6 批次 5 · 核销批

- 对照 `docs/Sprint4-全库审查修复计划-2026-09-19.md`（W-A2 剩余范围 = 规则4/18/3 豁免 +
  批次A 测试缺口）与 `docs/Sprint4完工审查报告-2026-09-19.md` 遗留行动项 1~3，逐条核销。
- W-A2①③ 豁免补齐须附回归用例（规则4/18/3 各一正一负）。
- 产出 `docs/工程清债核销清单-2026-09-20.md`（勾销表：条目/处置/证据/commit），无法核销
  的条目写明原因留档，**不粉饰**。

### 3.7 批次 6 · bundle 证据字段合入（对冲② + 缠论标签）

- 字段只增：对冲稳定配对清单（批次 1 生成函数，月末重估缓存）+ 配对触发日 watch 提示
  （A 急跌/B 同涨，供 LLM watch→buy 参考，**不做机械调权**）+ 缠论结构状态摘要（批次 2
  标签流，每票一行）。
- 实现约束：bundle 版本化照 Sprint4 批次C 惯例；token 预算先砍证据墙再砍本字段；
  **决策策略语义零变化**（字段仅进上下文，不进规则引擎）；测试进既有 test_ai_pipeline.py。
- 依赖：批次 1、2 产出 + 批次 4 的 ai 域修复落地。

### 3.8 批次 7 · 结题 + 录制器首周体检（2026-09-25 盘后条件触发）

- 录制器体检：minute_snapshot 首周行数/连续性缺口/各票覆盖、recorder_health.json 冷却
  是否生效、T7 周度清理不误删、5 分钟粒度对日线 OHLC 交叉抽验（≥10 票）。
- 汇总回填本方案 §6（三条研究线 gate 数字/披露统计 + 清债核销计数 + 体检结果 +
  VERDICT），更新 docs/策略库.md 与项目记忆，最终汇报。
- DoD③ 生产两日验证（09-21/22）结果一并收录。

## 4. 批次拆分与多 agent 编排

| 批次 | 内容 | 验收（不过即停/归档） | 并行性 |
|---|---|---|---|
| 0 qfq 数据层 | 审计 A1 → Gate 0-A~0-D → open_qfq 补列 + fetcher 增量路径 + run_all 预注册 4 测试名 + 周一 crontab 首 tick 自检 | Gate 0-A~0-D 全过；无解释畸变复检不过 → 上报停批 | **串行先行**（后续全部依赖修复后数据） |
| 1 对冲研究批 | signals/hedge_pair_research.py + test_hedge_pair.py | Gate H0~H3；fail 归档该线（配对清单照产） | 与 2、3 并行 |
| 2 缠论证据批 | signals/chan_evidence.py + test_chan_evidence.py | 单测绿 + 全池实跑披露统计 + 3 票样例 | 与 1、3 并行 |
| 3 X 批次 | signals/rotation_x.py + test_rotation_x.py | X1/X2 各自三件套；fail 归档该轴 | 与 1、2 并行 |
| 4 P2 清债 | 4a/4b/4c 三 agent 分域（§3.5） | run_all 全绿 + 逐项回归用例 + 勾销记录 | 依赖 0；可与 1/2/3 错峰并行（文件域不相交：研究只加新文件） |
| 5 核销批 | W-A2①③ + 行动项核销 → 勾销清单文档 | 核销清单 + 回归用例绿 | 依赖 4 |
| 6 bundle 合入 | 对冲② + 缠论标签字段 | 字段只增 + 版本化 + test_ai_pipeline 绿 | 依赖 1、2、4；与 5 并行（域不相交） |
| 7 结题+体检 | §3.8 | §6 回填 + 策略库/记忆 + 体检报告 | **最后，2026-09-25 盘后触发** |

- 提交：每批次一 commit（中文信息、前缀 `批次N(全量批)`，照仓库惯例；**批次编号与
  Sprint4 批次A~D、C-ARC 批次A/B 无关，勿混用**）；git add 仅本批次文件、禁止 `add -A`、
  无 pathspec 不裸 commit（R3 并行惯例）；commit 前 tests/ 全量绿。
- 并行纪律：同 worktree 多 agent 时文件所有权严格分区（研究批只加 signals/ 新文件与
  预注册新测试文件；P2 各域 agent 只动本域文件）；遇 index.lock 冲突等数秒重试。

## 5. 工程守则（执行会话红线）

1. 解释器一律 `.venv/bin/python3`；无 scipy，只用 numpy/pandas/标准库。
2. **唯一授权写库的批次是批次 0**（qfq 列族 + open_qfq 新列，备份/单事务/体检/回滚四件
   齐套）；其余所有批次与研究脚本 market.db 一律
   `sqlite3.connect("file:...market.db?mode=ro", uri=True)` 只读，不写任何库表。
3. 测试隔离：临时目录 + AGSICKLE_* 环境变量隔离，绝不触生产库表（生产拥挤已激活，
   不隔离必失败且写回生产）。
4. 判据先定后跑：§3 各线口径自批次 0 commit 后不可变；任一 gate fail 即归档该线/该轴，
   禁止调参/换口径重跑；缠论证据批无 gate 但口径重钉同样冻结。
5. 不动 config.json、不动既有 crontab 条目、不动 webapp（除非 P2 条目明确涉及）。
6. 在线取数（除权清单/申万映射）失败即阻断上报，不落库、不 Mock 过关。
7. 结题如实回填 §6，不粉饰；每批独立 commit 可追溯。

## 6. 结题结果（待批次 7 实跑后回填）

（空）
