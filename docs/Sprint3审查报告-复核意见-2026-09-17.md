# Sprint3 多 agent 审查报告 · 独立复核意见

> 复核日期：2026-09-17 11:xx（原报告出具后 ~8 小时）
> 复核对象：`docs/Sprint3多agent审查报告-2026-09-17.md`（21 条指控 = 9 P0 + 12 P1 + 若干 P2）
> 复核方法：**4 个独立子代理并行核实** + 主 agent 亲验测试基线/生产库/git 状态 + 联网核实 akshare 与腾讯接口文档 + sqlite 只读实证
> 复核原则（按用户偏好）：**区分「理论缺陷」与「生产事故级」**；**先查 import 图与既有防护链再下结论**；**不臆断**
> **一句话结论**：**原报告 21 条指控中 19 条属实、1 条半属实、1 条举例小瑕、0 条不属实；且原报告漏报了 1 条比 P0-3 更严重的现役生产事故（launchd catchup 整体瘫痪 161 次全失败），P0-6 腾讯字段错位比原描述更严重（f[42] 根本不是市值而是"当日最低价重复"），P1-3 实际是 4 处漏改而非 3 处；综合 4 agent 判定，Sprint 3 不通过结论成立，最严重的三条是 P1-6（skip_gate 直写已接通）、P0-1（应急扫描缺条件①）、catchup launchd 瘫痪（原报告漏报）。**

---

## §0 复核仪表盘

### 基线核实（主 agent 亲验）

| 项 | 原报告声称 | 实测 | 结论 |
|---|---|---|---|
| 测试用例数 | 267/267 通过（3.31s） | `.venv/bin/python3 -m pytest tests/ --collect-only -q` → **267 tests collected** ✅；`tests/run_all.py` → 16/16 文件通过（9.2s） | **属实** |
| git 变更范围 | 39 文件，+3900/−269 | `git diff --stat HEAD` → **39 files changed, 3900 insertions(+), 269 deletions(-)** | **属实** |
| risk_event 今日污染 | limit_halt_emergency ×70 + factor_crowding_active ×1 | 主 agent 亲验：`limit_halt_emergency=80`、`factor_crowding_active=1`、`watchdog_stale=2`、`profile_verdict_red_line=1`；Agent B 跑完 test_risk_engine.py 后**又增到 108** | **属实且更严重**（每次跑测试都在追加） |
| signal 表 v1+v2 双份 | 53,151 + 55,254 = 108,405 | sqlite 实测：`reversal_lowvol=53151`、`reversal_lowvol_v2=55254`、total=**108405** | **精确吻合** |
| breadth_daily 今日行 | advance_decline_ratio=None | sqlite 实测：`{'date': '2026-09-17', 'limit_up_count': 0, 'advance_decline_ratio': None, 'breadth_composite': None, 'source': 'em'}` | **精确吻合** |
| 生产 position/kill_switch | 未提及 | 实测：**零持仓、limit_halt_stuck 空、kill_switch=0、emergency_scan 决策单 0 条** | Agent A 补充：D1 缺陷**已接线但休眠**，非"已造成损失" |

### 21 条指控的复核判定汇总

| # | 编号 | 原报告判定 | 复核判定 | 严重性重估 |
|---|---|:---:|:---:|---|
| 1 | P0-1 应急扫描缺条件① | 属实 | ✅ **属实** | 生产事故级（已接线 postclose+intraday 双 cron，休眠因零持仓） |
| 2 | P0-2 睡前 confirm 断裂 | 属实 | ✅ **属实** | 生产事故级（fail-safe 方向：让 D1 对真跌停票失效） |
| 3 | P0-3 09:14 failsafe 无调度 | 属实 | 🟡 **半属实** | 结论对但推理错；**漏报更严重根因：launchd catchup 整体瘫痪** |
| 4 | P0-4 engine.check() 写生产库 | 属实 | ✅ **属实** | **降级为审计事故**（两个 rule 名全仓无决策读方） |
| 5 | P0-5 earnings 黑名单拦 SELL | 属实 | ✅ **属实** | 生产事故级（叠加 P1-1 后单条负面新闻即触发） |
| 6 | P0-6 腾讯 ask1 字段错位 | 属实 | ✅ **属实且更严重** | 生产事故级（f[42] 是"最低价重复"不是市值；f[45]/f[46] 也错位） |
| 7 | P0-7 stock_zh_a_gdhs 接口错 | 属实 | ✅ **属实** | 功能半失效（权重重归一后 composite 仍出） |
| 8 | P0-8 breadth 盘前采集 | 属实 | ✅ **属实** | 生产事故级（极端避险档永不触发） |
| 9 | P0-9 UI 零展示 | 属实 | ✅ **属实** | 验收缺口（非生产事故级） |
| 10 | P1-1 关键词嵌套双计 | 属实 | ✅ **属实**（举例小瑕） | P0-5 引爆器（"业绩预亏⊃预亏"举例错误，"预亏"不在列表） |
| 11 | P1-2 stuck 计数双缺陷 | 属实 | ✅ **属实** | 潜伏放大器（catchup 修好后单日膨胀 14 倍） |
| 12 | P1-3 signal profile 未过滤 | 三处 | ✅ **属实**（实为**四处**） | 生产事故级（拥挤度 IC 混口径污染规则 20） |
| 13 | P1-4 IVOL 基准对齐 | 属实 | ✅ **属实** | 评估失真（仅影响 v2 回填历史，实盘无前视） |
| 14 | P1-5 backfill 用当前 state | 属实 | ✅ **属实** | 评估失真（仅影响 v1，v2 硬编码豁免） |
| 15 | P1-6 skip_gate 直写 | 属实 | ✅ **属实** | **极高**（HEAD 已生效并被测试锁定，非"本次接通"） |
| 16 | P1-7 health/blacklist scope 收窄 | 属实 | ✅ **属实** | 口径背离但数据未漏检（内部先跑了全库） |
| 17 | P1-8 GateCard 不显示委托价 | 属实 | ✅ **属实** | 生产事故级（叠加 P1-9 时应急单盲操作） |
| 18 | P1-9 确认无二次确认 | 属实 | ✅ **属实** | 生产事故级（否决反而有 Dialog，保护等级倒挂） |
| 19 | P1-10 stuck 用 daily_bar 昨收 | 属实 | ✅ **属实** | 在线（14:50 cron 每日跑） |
| 20 | P1-11 文案 15→21 条 | 属实 | ✅ **属实** | 验收缺口（应急单确认页谎报风控范围） |
| 21 | P1-12 decision_id null | 属实 | ✅ **属实** | 理论缺陷（后端 int(0) 通过校验但 runner 会失败） |

**统计**：属实 19 条 · 半属实 1 条 · 举例小瑕但结论对 1 条 · 不属实 0 条 · **原报告漏报的额外发现 5 条**（见 §3）。

---

## §1 逐条复核详情（按严重性排序，不按原报告编号）

### 🥇 最严重 · P1-6 skip_gate 直写路径已接通 —— ✅ 属实（Agent B 判定"极高"）

**位置**：`risk/engine.py:495-504` + `execution/runner.py:586-618`

**关键代码**：
```python
# risk/engine.py:495-504
if decision.get("emergency_scan"):
    v.warnings.append("跌停应急扫描单：%s（confirm 优先，09:14 未确认自动执行）" % detail)
    return
# Sprint 2 任务 4：加 skip_gate 标志，让 runner.py 走直写 B 路径
decision["skip_gate"] = True
decision["confirmed_by"] = "emergency_rule21"
v.warnings.append("跌停应急路径激活：%s（自动成交：跳过 confirm 闸门）" % detail)
```

```python
# execution/runner.py:586-618
elif exec_cfg.get("manual_gate", True) and not decision.get("skip_gate"):
    path = _write_pending(...)   # 走人工闸门
else:
    if decision.get("skip_gate"):
        print("[gate] 直写执行（skip_gate=%s...）" % ...)
        confirmed_by = decision.get("confirmed_by") or "emergency_rule21"
    _execute(conn, decision_id, decision, ...)   # 直接落 trade 表
```

**Agent B 独立实测**：在 P0-5 反例中，SELL 600519 @1341.00（跌停价）+ 浮亏 10.6% + 无 emergency_scan，engine.check() 输出 warnings 已含"自动成交：跳过 confirm 闸门"，`decision["skip_gate"]=True` 已被写入。**只是碰巧被 blacklist violation 拦下**——如果没有 P0-5 的黑名单命中，这单会直接跑进 `_execute()` 自动成交。

**Agent B 校正原报告**：原报告说"HEAD 中该 flag 无消费方（闸门必拦），本次使其生效"——**不准确**。`runner.py:586,594` 已经**是**消费方，且 `tests/test_execution.py:384-412 test_propose_skip_gate_executes_directly_with_emergency_marker` 明确断言"skip_gate=True 时不应写 pending"且"status 必须 executed 系列"——**当前 HEAD 就已生效且被测试锁定**。任何回滚需要同时改测试。

**严重性重估：极高，生产事故级**
- 触发条件真实可达：LLM 完全可能在连续跌停+浮亏场景下主动生成"跌停价 sell"决策（这是止损常识）
- 一旦触发：**绕过人工闸门、绕过 pending 文件、直接调 `_execute()` 落 trade 表**
- 无 kill switch、无 config flag、无环境变量可关闭
- 违背项目"绝不自动成交"总原则（`docs/决策策略与工作流.md` 反复强调人工闸门是唯一写操作）

**修法**：`config.json` 新增 `execution.emergency_direct_exec: false` 开关；`runner.py:594` 分支加 `if not exec_cfg.get("emergency_direct_exec", False): fall_through_to_gate()`；同步改测试。

---

### 🥈 次严重 · P0-1 应急扫描缺"当日跌停"条件① —— ✅ 属实（Agent A 判定"根缺陷"）

**位置**：`signals/limit_halt.py:104-123` + `risk/engine.py:410-444`

**关键代码**：
```python
# signals/limit_halt.py:104-123
hit, info = _limit_halt_condition(code, ctx, cfg)
if not hit:
    continue
down = info["down"]
# ...全段无任何 latest_prices[code] 与 down 的比对...
"order": {"side": "sell", "price": float(down), "shares": avail},
"emergency_scan": True,
```

```python
# risk/engine.py:410-444（共用函数）
① 死封候选：跌停价（由调用方比对 order.price == down，此处只算出 down）
down = limit_price(float(pc), pct, up=False)
if loss is None or loss < stop_loss_line(ctx, cfg, code) - 1e-12:
    return False, {"reason": "浮亏未破止损线", "down": down}
```

**Agent A 独立实测**（`/tmp/repro_p01.py`，昨收 100 / 今收 103 / 成本 130）：
```
生成应急单数量: 1
  code=600519 order={'side':'sell','price':90.0,'shares':200} emergency_scan=True
  reasons=['规则21主动扫描：跌停封死且浮亏 20.8% ≥ 止损线 8.0%', ...]
跌停价 down=90.0   今收 latest=103.0   今收==跌停价? False
```
今收 103（+3%，**远离跌停 90**），仍生成 `sell @90.0` 应急单，且 reason 文案**谎称"跌停封死"**。

**接线图核实**（Agent A 查 import 图与既有防护链）：
- `pipeline/postclose.py:258-260` 步骤3.5 `r = limit_halt.run_postclose_scan()` → 15:30 cron 每日跑
- `pipeline/intraday_check.py:113` 步骤3.6 `run_intraday_scan` → 14:50 cron 每日跑
- `logs/pipeline.log` 实测有多条"步骤3.5 跌停应急扫描：scanned=0"（scanned=0 仅因当前零持仓）
- `logs/reports/intraday-2026-09-1{1,4,5,6}.md` 实证 14:50 扫描在跑

**严重性重估：生产事故级（已接线、休眠待发）**
- 后果链：**任一**破线持仓（不论是否跌停）→ postclose 生成次日 `sell@跌停价` approved 单 + 谎报"连续跌停"通知；intraday 把它计入 stuck → `enforce_stuck_rules` `len(rows)>=3` → **全账户 kill_switch 停机 72h**
- 即"3 只普通亏损票冻结全账户"属实且已接线
- 唯一"减害"：指控的"09:14 failsafe 自动强平"腿当前断（见 P0-3），故**无人值守自动卖出**暂不会发生
- 但人工 confirm 会按实时价市价卖出非跌停亏损仓（`runner.py:734` 用 `broker.latest_price`，非 90）

**修法**：`scan_positions` 命中后补条件① `lp = latest_prices.get(code); if lp is None or lp > down * (1 + 0.005): continue`；补"未跌停但破线不命中"测试。

---

### 🥉 第三严重 · launchd catchup 整体瘫痪 —— ⚠️ **原报告漏报的现役生产事故**

**Agent A 独立发现**（在核实 P0-3 时顺带查出）：

```
logs/catchup.log 161 行全为错误、0 次成功：
  141× /bin/zsh: can't open input file: .../A股镰刀手/deploy/run_catchup.sh
       （中文路径被 plist 编码错乱）
   20× /Applications/Xcode.app/.../python3: can't open file '.../catchup.py':
       [Errno 1] Operation not permitted  （TCC 授权）
心跳 logs/state/catchup_heartbeat mtime=2026-09-16 15:42（已停 ~20h，0 字节）
```

**影响范围**（三层兜底全废）：
1. bundle 刷新兜底：漏跑 premarket 时无法补齐
2. kill 安全网兜底：漏跑 intraday_check 时无法触发回撤 kill
3. 盘后补全兜底：漏跑 postclose 时无法补日报/盯市

**对原报告 P0-3 的修正**：
- 原报告说"catchup 重跑 premarket 的条件是 bundle 不新鲜或 signal 未对齐——09:00 premarket 成功后两者都不满足 → 09:14 后无人再调 failsafe"
- **Agent A 实测证伪**：catchup `:192` 查 `SESSION_DIR/<latest_td>/bundle.md`，而 premarket 写 `session/<run_date=今天>/bundle.md`。盘中 `latest_td=T-1 ≠ 今天`，故 catchup 查的是**昨天的 bundle（必然不新鲜）** → `need_premarket=True` → 若 catchup 健康，反而会每 30 分钟重跑 premarket 并以 `do_exec=True` 触发 failsafe
- **真正根因**：launchd catchup 因中文路径编码 + TCC 授权**整体瘫痪**，所以 09:14 后确实无人再调 failsafe——但根因不是"条件不满足"而是"catchup 死了"
- **结论对、理由错、且低估了根因范围**

**严重性重估：现役生产事故（比 P0-3 更严重）**
- 此刻正在进行：三层兜底全废已 ~20h
- 修法：先修 launchd 中文路径编码（用 file:// URL 或 base64）+ TCC 授权（`~/Library/Application Support/com.apple.TCC/`），让 catchup 复活；复活后既有 bundle 路径错配反而会让 failsafe 被（脆弱地）触发
- **建议升级为独立 P0**，排在 P0-3 之前

---

### P0-2 睡前 confirm 腿断裂 —— ✅ 属实

**位置**：`signals/limit_halt.py:155` + `execution/runner.py:689-698`

**关键代码**：
```python
# signals/limit_halt.py:155（run_date = 自然日 +1，非交易日历）
run_date = run_date or (now + timedelta(days=1)).strftime("%Y-%m-%d")
# 注释（:153-154）自称"节假日场景由 premarket 兜底…自然顺延"，与实现相反

# signals/limit_halt.py:176-177（通知诱导睡前 confirm）
"。已生成应急卖单（sleep 前 confirm 可次日 09:15 执行；"
"否则次日 %02d:%02d 自动执行）" % FAILSAFE_TIME

# execution/runner.py:689-698（跨日闸门，无 emergency_scan 豁免）
run_date = str(row[1] or "")
if run_date and run_date != now.strftime("%Y-%m-%d"):
    _set_status(conn, decision_id, "expired")
    _remove_pending(decision_id, orders_dir)
    return None

# signals/limit_halt.py:290-293（次日 failsafe 只查 approved AND run_date=今天）
"SELECT id, code FROM decision WHERE emergency_scan=1"
" AND status='approved' AND run_date=? ORDER BY id"
```

**Agent A 独立实测**（`/tmp/repro_p02.py`）：
```
盘后15:30生成单: id=1 run_date=2026-09-18 status=approved emergency_scan=1
当晚22:00 confirm (now=09-17, run_date=09-18):
  [confirm] decision#1 状态 -> expired（决策日 2026-09-18 ≠ 今日）
  confirm 返回: None ; confirm 后 status = expired
次日(09-18) failsafe 可查到的 approved 单数量: 0
  failsafe 输出: {'pending': [], 'executed': [], 'notified': False}
```

**周末搁浅**：run_date 用自然日，周五盘后 → run_date=周六；catchup `:139` 非交易日秒退、premarket 不跑 → 周末无 notify 无兜底；周一 failsafe 查 run_date=周一 ≠ 周六 → 查不到 → 单以 **approved 永久滞留**（无任何清理路径）。

**严重性重估：生产事故级（fail-safe 方向）**
- 不会造成误卖，反而**让 D1 对真正的连续跌停票失效**——用户照通知"睡前 confirm"，单子被静默 expired，次日真跌停仓**卖不出去**，恰好复现 D1 要破解的死锁
- 与 P0-3 叠加：confirm 腿 + 自动执行腿**双双断裂** → D1 对真阳性完全不可用
- confirm 仅写 risk_event + print，**无 macOS notify**（静默作废）

**修法**：`confirm()` 对 `emergency_scan=1` 豁免跨日闸门（run_date 语义=预期执行日）；run_date 改用 `data/trade_cal.py` 取下一交易日；补"当晚 confirm → 次日执行"集成测试。

---

### P0-5 业绩预告黑名单连 SELL 一起拦 —— ✅ 属实

**位置**：`risk/blacklist.py:42-71` + `risk/engine.py:134-139` + `ai/decide.py:78-82`

**关键代码**：
```python
# risk/blacklist.py:42-71（无 action 参数）
net = earn_net.get(code)
if net is not None and net <= EARNINGS_NEG_NET_LIMIT:
    reasons.append(f"业绩预告负面（net={net}）")
result[code] = (len(reasons) == 0, "; ".join(reasons) or "-")

# risk/engine.py:134-139（rule_blacklist 不看 action）
def rule_blacklist(decision, ctx, cfg, v):
    code = str(decision.get("code") or "")
    item = (ctx.blacklist or {}).get(code)
    if item and not item[0]:
        v.violations.append("黑名单：%s 被拦截（%s）" % (code, item[1]))

# ai/decide.py:78-82（validate 同样不看 action）
if blacklist:
    item = blacklist.get(code)
    if item is not None and not item[0]:
        errors.append("code %s 在黑名单中: %s" % (code, item[1]))
```

**对比证据**（报告称"与自身原则矛盾"）—— `risk/engine.py:604-611` 的 `rule_factor_crowding` 明确豁免 sell：
```python
def rule_factor_crowding(decision, ctx, cfg, v):
    """规则20：因子拥挤度熔断... sell 单不挡（拥挤熔断不该堵止损）。"""
    if decision.get("action") != "buy":
        return
```

**Agent B 独立实测**（反例复现，`AGSICKLE_DB=/tmp/p05_repro.db` 隔离）：
```
check_blacklist['600519'] = (False, '业绩预告负面（net=-3）')
validate(SELL 600519) -> ok=False  errors=['code 600519 在黑名单中: 业绩预告负面（net=-3）']
engine.check(SELL 600519) approved=False
  violations=['黑名单：600519 被拦截（业绩预告负面（net=-3））']
  warnings=['跌停应急路径激活：600519 跌停应急: 卖价=1341.00...（自动成交：跳过 confirm 闸门）',
            '跌停保护豁免：limit_halt_emergency 应急单放行']
```

**Agent B 关键新发现**：SELL 单**同时命中规则 21 与规则 1**——规则 21 已设 `skip_gate=True` 且 warning 打印"自动成交：跳过 confirm 闸门"，但被规则 1 的 violation 一票否决。原报告"rule_blacklist 先于规则 21"表述**顺序不精确**（实际两条规则都跑，最终 approved 由 violations 非空判定），但结论"应急单被拒"**正确**。

**严重性重估：生产事故级**
- 与 P1-6 的悖论：如果**没**命中黑名单，同样条件的 sell 单会被规则 21 自动 skip_gate 直写成交（P1-6）；一旦命中黑名单，止损出口彻底焊死。**资金风险两头都是坑**
- **不会误卖，但会误留**：连续跌停+负面预告恰是必须走的场景，此时拒绝 sell = 强制持仓吃后续跌幅
- **修复顺序必须"先加 P1-6 开关，再放 P0-5 sell 豁免"**，否则修 P0-5 反而放大 P1-6

**修法**：对 `action=="sell"`（含 emergency 单）豁免业绩预告类黑名单项（保留 ST 等原有项现状）。

---

### P0-6 腾讯行情字段错位 —— ✅ 属实且比原描述更严重

**位置**：`data/quotes.py:107-113`

**Agent C 联网直连 qt.gtimg.cn 实测**（2026-09-17 11:08 抓 sh600519 真实报文）：

| 索引 | 真实值 | 真实语义 | 代码当作什么 | 判定 |
|---|---|---|---|---|
| f[3] | 1264.00 | 当前价 | price | ✓ |
| **f[19]** | **1264.01** | **卖一价** | 未用 | ✗ 漏用 |
| **f[20]** | **3** | **卖一量（手）** | 未用 | ✗ 漏用 |
| **f[21]** | 1264.06 | **卖二价** | ask1_price | ✗ 错位 |
| **f[22]** | 1 | **卖二量** | ask1_vol | ✗ 错位 |
| f[30] | 20260917110847 | 时间戳 | time | ✓ |
| f[33] | 1265.98 | 最高 | high | ✓ |
| f[34] | 1254.00 | 最低 | low | ✓ |
| **f[42]** | **1254.00** | **当日最低价（重复）** | float_mv "元" | ✗✗ 完全拿错 |
| f[44] | 15801.03 | **流通市值（亿元）** | 未用 | ✗ 漏用 |
| **f[45]** | 15801.03 | 总市值（亿元） | limit_up | ✗ 错位 |
| **f[46]** | 6.29 | 市净率 | limit_down | ✗ 错位 |
| f[47] | 1383.80 | **涨停价** | 未用 | ✗ 漏用 |
| f[48] | 1132.20 | **跌停价** | 未用 | ✗ 漏用 |

**Agent C 校正原报告**：原报告说 f[42] "流通市值单位是亿元却按元用"——**实际更糟**：f[42] **根本不是市值**，而是**当日最低价的重复**。所以：
- `float_shares = 1254.00 / 1264.00 ≈ 0.99` 股（茅台应有 ~12.5 亿股）
- `seal_ratio = ask1_vol × 100 / 0.99 ≈ 卖二量 × 101` → 只要卖二有 1 手挂单，seal_ratio 就 >3%（1×100/0.99=101=**10100%**）
- **条件②恒 True**；即使 f[21]/f[22] 为空（涨停封板时），走 `return True` 分支仍恒 True

**原报告未提及的额外错位**：`limit_up=f[45]` 实际拿到总市值亿元、`limit_down=f[46]` 实际拿到市净率——两字段彻底无效。所幸规则 21 走 `common.market.limit_price()` 公式重算，不依赖 quote.limit_up/down，未直接放大事故；但 quote 输出任何下游（如 bundle、webapp）若展示这两字段都是错的。

**测试盲区自证循环** —— 属实：`tests/test_quotes.py:35-61` fixture 把**与实现同款的错索引**写进报文构造，`assert q["float_mv"] == 1640123456789.0` 用茅台万亿流通市值×100 作为期望值——**纯自证循环**，不可能发现字段错位。

**tx 兜底源连锁失效** —— 属实：`data/breadth.py:141-144` `if q.get("ask1_price") == up: zt += 1`，ask1_price 实际是卖二价，涨停封板时卖二为空 → **zt/dt 恒 0**。

**严重性重估：生产事故级**
- 规则 21"跌停封单应急"条件② 死封判定**恒真**，条件①③ 一旦满足就直接放行卖单——本应拦截"死封无法成交"的保护失效
- tx 兜底源涨停家数**恒 0**——所幸 em 主源优先，仅在 em 挂时暴露
- 已上线的 quote 字段 `ask1_price/ask1_vol/float_mv/limit_up/limit_down` **全部语义错误**

**修法**：ask1 改 f[19]/f[20]；float_mv 改 f[44] × 1e8；limit_up/down 改 f[47]/f[48]；fixture 改用真实报文样本（Agent C 已抓 sh600519 与 sz000001 双票交叉验证）。

---

### P0-8 breadth 盘前采集当日 —— ✅ 属实

**位置**：`pipeline/premarket.py:204-214` + `data/breadth.py:58-62, 198, 308-311`

**关键代码**：
```python
# pipeline/premarket.py:204-214（步骤 6.7，唯一生产调用点）
try:
    from data import breadth
    br = breadth.fetch_breadth_daily(conn=conn)  # ← 未传 date_str → 默认今天

# data/breadth.py:308-311
def fetch_breadth_daily(date_str=None, conn=None):
    if date_str is None:
        date_str = datetime.now().strftime("%Y-%m-%d")

# data/breadth.py:58-62（池空当成功）
df_zt = call_ak("zt_pool_em", ak.stock_zt_pool_em, date=date_str)
if df_zt is not None and df_zt.empty:
    out["limit_up_count"] = int(len(df_zt))
else:
    out["limit_up_count"] = 0   # ← 空池 → 0（不抛错）

# data/breadth.py:198（成功判定）
if out.get("limit_up_count") is not None or out.get("limit_down_count"):
    return out   # limit_up_count=0 满足 is not None → 视为成功，不走 tx/sina 兜底
```

**全仓调用点**（Agent C grep 验证）：
```
pipeline/premarket.py:208  ← 唯一生产调用点
pipeline/postclose.py       ← 无匹配（grep breadth/market_activity/advance_decline 全空）
```

**生产实证**：`breadth_daily` 2026-09-17 行 `{'limit_up_count': 0, 'advance_decline_ratio': None, 'breadth_composite': None, 'source': 'em'}` ✅ 与报告完全吻合。

**严重性重估：生产事故级**
- 假 0 行落库后，`breadth_composite < -2 → override_cap=0.1` 极端避险档**永远不会触发**
- 每日盘前写假 0，历史 z 窗口（250 日）长期被污染 → 冷启动 60 日门槛过后 std 失真 → **breadth 信号维度实质报废**
- regime 与 bundle 全天读假 0 行，LLM 决策输入的"市场情绪"永远是"零涨停零跌停"

**修法**：breadth 采集移到 `pipeline/postclose.py`（收盘后）；`_fetch_breadth` 区分"池为空"与"失败"（空池应触发 tx/sina 兜底而非视为成功）。

---

### P1-3 signal 表 profile 未过滤 —— ✅ 属实（**实为四处漏改**）

**生产实证**：`SELECT profile, COUNT(*) FROM signal GROUP BY profile` → `reversal_lowvol=53151`、`reversal_lowvol_v2=55254`、total=**108405** ✅ 与原报告精确吻合。

**Agent C 查出的四处漏改**（原报告写"三处"）：

| # | 位置 | 代码 | 影响 |
|---|---|---|---|
| 1 | `signals/signals.py:566-570` | `SELECT code, as_of, score FROM signal WHERE score IS NOT NULL` | 拥挤度 IC 混口径 → **规则 20 熔断输入污染** |
| 2 | `webapp/api/market.py:160-164` | `/api/signals` JOIN 无 profile 过滤 | 一票两行混排返回给 LLM/前端 |
| 3 | `webapp/api/workflow.py:61` | `SELECT MAX(as_of), COUNT(*) FROM signal` | 工作流面板 COUNT 翻倍 |
| 4 | `webapp/api/data_status.py:67` | 同上 | 数据状态页 COUNT 翻倍 |

**对比参考**：`review/signal_eval.py:66-72` **已按 profile 过滤**（计划 §6.2 唯一改到的地方）：
```python
from signals.signals import profile as _profile
prof = _profile()
"SELECT code, as_of, signals, score FROM signal"
" WHERE profile = ? OR profile IS NULL", (prof,)
```

**严重性重估：生产事故级**
- 拥挤度 IC 混口径 → **规则 20 因子拥挤熔断**输入被污染 → 影响实盘风控权重降级判定
- /api/signals 一票两行 → LLM 决策输入含混口径信号 → 决策质量下降
- workflow/data_status COUNT 翻倍 → 运维面板误导

**修法**：四处 SQL 全部加 `WHERE profile=?` 过滤（读 `config.signals.profile`）。

---

### P0-4 engine.check() 写生产库 —— ✅ 属实但严重性应降级

**位置**：`risk/engine.py:707, 750-758, 652-668`

**关键代码**：
```python
# risk/engine.py:707（check() 签名无 conn 参数）
def check(decision: dict, ctx: RiskContext, cfg: dict) -> Verdict:

# risk/engine.py:750-758（emergency_pending 落库路径）
if v.emergency_pending is not None:
    try:
        from data.fetcher import get_conn  # 局部导入避免循环
        conn = get_conn()
        try:
            record_event(conn, v.emergency_pending["rule"], v.emergency_pending["detail"])
        finally:
            conn.close()

# data/fetcher.py:241-243（get_conn 硬编码路径，仅 AGSICKLE_DB 逃生门）
def get_conn() -> sqlite3.Connection:
    db_file = os.environ.get("AGSICKLE_DB") or BASE / CFG["db_path"]
```

**Agent B 独立实测**（BEFORE/AFTER 生产库计数）：
```
BEFORE 跑 tests/test_risk_engine.py: limit_halt_emergency=105, factor_crowding_active=2
运行:  46 passed in 0.27s
AFTER : limit_halt_emergency=108, factor_crowding_active=2
```
- 单次 pytest 直接写入 **+3 条 limit_halt_emergency** 到生产 `data/market.db`
- detail 内容与 `tests/test_risk_engine.py:600, 617-622` 的 seed 完全一致（`600519 跌停应急: 卖价=1341.00`）——真实生产 600519 跌停价不可能是 1341
- `factor_crowding_active` 保持 2 未增：**恰好实证** engine.py:660-662 的同日去重把新写入压掉了——今日 00:48 已有 1 条测试残留 → 后续任何真实拥挤熔断事件都写不进 risk_event

**Agent B 严重性重估（关键校正）**：**中等——审计完整性事故，非决策正确性事故**
- ✅ 报告"测试污染生产库"完全属实
- ⚠️ 但报告"factor_crowding 同日去重被压住 → 真实事件被吞"需**降级**：`rule_factor_crowding` 的实际风控行为（把 target_weight 压回 5%）**不依赖 risk_event 表**——risk_event 只是留痕。真实事件被吞的是**审计行**，风控本身仍生效
- ⚠️ grep 全仓：`limit_halt_emergency` / `factor_crowding_active` 两个 rule 名**均无决策读方**（`signals/signals.py:520` 读的是 `factor_crowding_state`；`signals/limit_halt.py:232` 读的是 `limit_halt_stuck`；`runner.py:212` 读的是 `kill_switch/kill_manual/kill_extend`）——**测试污染不会污染任何自动决策路径**
- 🔴 真实影响：(a) 审计溯源不可信（108+ 假日志混入）；(b) 若未来有人加"读 limit_halt_emergency 计数做 stuck 判定"的代码，会立即被测试残留击穿；(c) 每次跑 CI 都污染，规模无上限

**修法**：落库挪到持有 conn 的调用方，或 RiskContext 加 conn 字段；短期统一让测试套件设 `AGSICKLE_DB` 指向临时库；**清理生产库今日 108+ 条测试残留**。

---

### P0-3 09:14 failsafe 无调度触发者 —— 🟡 半属实

**属实的部分**（Agent A 核实）：
- 全仓 grep `premarket_failsafe` 生产唯一调用点 = `pipeline/premarket.py:93` ✅
- premarket 09:00 cron 触发，`limit_halt.py:298-300` `do_exec = now>=09:14` → 09:00/09:09 时 **False → 仅 notify** ✅
- 无专用 09:14 launchd/cron ✅

**不属实的部分（原报告推理错误）**：
- 原报告称"catchup 重跑 premarket 的条件…09:00 premarket 成功后两者都不满足"
- **Agent A 实测证伪**：catchup `:192` 查 `SESSION_DIR/<latest_td>/bundle.md`，而 premarket 写 `session/<run_date=今天>/bundle.md`。盘中 `latest_td=T-1 ≠ 今天`，故 catchup 查的是**昨天的 bundle（必然不新鲜）** → `need_premarket=True` → 若 catchup 健康，反而会每 30 分钟重跑 premarket 并以 `do_exec=True` 触发 failsafe

**真正根因**：launchd catchup **当前完全瘫痪**（见 §1 第三条），所以 09:14 后确实无人再调 failsafe——但根因不是"条件不满足"而是"catchup 死了"。**结论对、理由错、且低估了根因范围**。

**修法**：
1. **先修 launchd catchup**（中文路径编码 + TCC 授权）—— 这是独立 P0
2. catchup 复活后，既有 bundle 路径错配反而会让 failsafe 被（脆弱地）触发；建议同时修 bundle 路径对齐
3. 或加 09:14 专用 launchd 任务（更稳）

---

### 其余 12 条简要复核（全部属实）

| # | 编号 | 判定 | 关键证据 | 严重性 |
|---|---|:---:|---|---|
| 7 | P0-7 stock_zh_a_gdhs 接口错 | ✅ 属实 | akshare 1.18.94 docstring 明确"股东户数"；正确接口 `stock_market_activity_legu` 实测返回"上涨/下跌/涨停/跌停"长表 | 功能半失效 |
| 9 | P0-9 UI 零展示 | ✅ 属实 | `webapp/api/` 无 breadth/earnings/limit_halt 模块；`grep emergency_scan webapp/frontend/src/` 命中数 0；`api_decisions`/`api_pending`/`workflow.traces` 均不透传 emergency_scan | 验收缺口 |
| 10 | P1-1 关键词嵌套双计 | ✅ 属实（举例小瑕） | 3 对嵌套精确匹配（POS: 预增⊂净利润同比预增、同比增长⊂净利润同比增长；NEG: 同比下滑⊂净利润同比下滑）；实测"公司净利润同比下滑30%"→pos=0 neg=2 即击穿阈值。**原报告"业绩预亏⊃预亏"举例错误**（"预亏"不在列表） | P0-5 引爆器 |
| 11 | P1-2 stuck 计数双缺陷 | ✅ 属实 | ① UPDATE 无 `WHERE last_attempt<today` 去重，实测同日调 14 次 stuck_days=14；② 未命中即 DELETE 行，实测连续 4 日命中后第 5 日未命中即清零 | 潜伏放大器 |
| 13 | P1-4 IVOL 基准对齐 | ✅ 属实 | `_hs300_close_series` 无 as_of 过滤返回全史最新；个股侧 `bars[bars["trade_date"]<=as_of]` 严格截断；反例推演 as_of='2024-06-01' 时基准结尾是 2026-09-17 → **前视偏差成立**。仅影响 v2 回填历史，实盘无前视 | 评估失真 |
| 14 | P1-5 backfill 用当前 state | ✅ 属实 | `_score_cross_section` v1 分支读 `read_factor_crowding().get("state")`；v2 分支硬编码 `degraded=False` 豁免。仅污染 v1 的 53151 行历史 | 评估失真 |
| 16 | P1-7 health/blacklist scope 收窄 | ✅ 属实 | `webapp/api/common.py:58,96` 默认 `scope="watchlist"`；但内部**先跑了全库 `check_blacklist`** 只是展示时过滤 → **数据未漏检**，只是 UI 展示窄 | 口径背离 |
| 17 | P1-8 GateCard 不显示委托价 | ✅ 属实 | GateCard 渲染字段清单无 `d.limit_price/d.price/d.shares/d.order/d.risk_notes/d.emergency_scan`；WorkflowPage.tsx:49 traces 映射硬编码 `risk_notes: []`；应急单 risk_notes 含"09:14 自动执行"关键警示**完全不可见** | 生产事故级隐患 |
| 18 | P1-9 确认无二次确认 | ✅ 属实 | 确认按钮单击直调 `onConfirm(Number(item.decision_id), by.trim())` 无 Dialog；否决按钮打开 Dialog 且理由必填——**破坏性操作与确认操作的保护等级倒挂** | 生产事故级 |
| 19 | P1-10 stuck 用 daily_bar 昨收 | ✅ 属实 | `run_intraday_scan` 不传 ctx_inputs → 走 `broker.prev_close`(=latest_close offset1) 与 `broker.latest_price(live=False)`(=latest_close offset0)，**全部读 daily_bar**；`intraday_check.py:74` 虽 force=True 拉了实时行情但 `:113` 未传进去 | 在线（14:50 cron 每日跑） |
| 20 | P1-11 文案 15→21 条 | ✅ 属实 | `TradesGatePage.tsx:74,104` + `workflow.py:98` 三处硬编码"15 条"；实际 `grep ^def rule_ risk/engine.py` 数出 **21 条** | 验收缺口 |
| 21 | P1-12 decision_id null | ✅ 属实 | `GateCard.tsx:156-162` disabled 只看 busy，未检查 `item.decision_id != null`；后端 `int(0)` 通过校验落到 runner 再失败 | 理论缺陷 |

---

## §2 P2 杂项复核（Agent D 核实 7 条）

| # | 项 | 判定 | 备注 |
|---|---|:---:|---|
| P2-a | `_persist_latest` 注释称 Path.replace 原子覆盖，实现是裸 write_text | ✅ 属实 | `review/signal_eval.py:395-421` 注释与实现背离；读端可能读到半成品 |
| P2-b | `enforce_stuck_rules(conn)` 缺省 now=None 时 TypeError | ✅ 属实（条件触发） | 所有已知调用点都传了 now，但函数签名允许 None → 未来踩坑 |
| P2-c | `read_breadth` 不校验行新鲜度 | ✅ 属实 | 数月前 composite 仍可触发极端避险档；与 P0-8 叠加放大 |
| P2-d | `FACTOR_CROWDING_PATH` 死常量零引用 | ✅ 属实 | 清洁度问题 |
| P2-e | `/api/decisions` 与 workflow traces 未输出 emergency_scan | ✅ 属实 | 与 P0-9 同源 |
| P2-f | 确认操作人为空默认 `by \|\| "human"` | ✅ 属实 | 审计链上无法区分"张三确认"和"匿名确认" |
| P2-g | 黑名单全 PASS 折叠时空表体 | ✅ 属实 | UI 观感问题 |

---

## §3 原报告漏报的额外发现（5 条）

复核过程中 4 个 agent 独立查出原报告**未提及**的问题：

### 漏报-1 · launchd catchup 整体瘫痪（**现役生产事故**，比 P0-3 更严重）
- `logs/catchup.log` 161 行全为错误、0 次成功；心跳 `logs/state/catchup_heartbeat` mtime=2026-09-16 15:42（已停 ~20h）
- 根因：中文路径被 plist 编码错乱（141 次）+ TCC 授权缺失（20 次）
- 影响：三层兜底全废（bundle 刷新 / kill 安全网 / 盘后补全）
- **建议升级为独立 P0，排在 P0-3 之前**

### 漏报-2 · 腾讯 f[42] 根本不是市值，是"当日最低价重复"（比 P0-6 描述更严重）
- 原报告说"单位错 1e8 倍"，实际是**字段完全拿错**
- f[45]/f[46] 也错位拿了总市值/PB 当涨跌停价（原报告未提及）
- 所幸规则 21 走 `common.market.limit_price()` 公式重算，未直接放大事故；但 quote 输出任何下游若展示这两字段都是错的

### 漏报-3 · P1-3 实际是**四处**漏改不是三处
- 原报告标题写"三处漏改"但列举了 4 个位置（signals.py + market.py + workflow.py + data_status.py）
- 代码上确实是 4 个 SQL 都要改

### 漏报-4 · P1-6 skip_gate 已在 HEAD 就生效并被测试锁定
- 原报告说"HEAD 中该 flag 无消费方（闸门必拦），本次使其生效"——**不准确**
- `runner.py:586,594` 已经**是**消费方，且 `tests/test_execution.py:384-412` 已有测试锁定该行为
- **不是"本次使其生效"，而是当前 HEAD 就已生效且被测试锁定**。任何回滚需要同时改测试

### 漏报-5 · P0-4 严重性应降级为"审计事故"
- 原报告说"K3 在 DB 侧被击穿"暗示决策级影响
- **Agent B 全仓 grep 证实**：`limit_halt_emergency` / `factor_crowding_active` 两个 rule 名**均无决策读方**
- 测试残留不会污染任何自动决策路径；真实伤害是审计溯源不可信 + 未来扩展风险
- 仍应修，但优先级低于 P0-5/P1-6

---

## §4 综合判定与修复顺序

### 原报告整体评价

**准确度极高**：21 条指控中 19 条属实、1 条半属实（P0-3 结论对推理错）、1 条举例小瑕（P1-1"业绩预亏⊃预亏"举例错误但整体结论对）、**0 条不属实**。这在第三方审查报告里是非常高的命中率。

**严重性分级基本合理**，但需两处校正：
- **P0-4 应降级**：从"K3 在 DB 侧被击穿"降为"审计完整性事故"（无决策读方）
- **P1-6 应升级**：从 P1 升为 **P0**（HEAD 已生效 + 被测试锁定 + 违背"绝不自动成交"总原则 + 无开关）

**漏报 5 条**，其中漏报-1（catchup launchd 瘫痪）是**现役生产事故**，比原报告 P0-3 更严重。

### 修复优先级建议（综合 4 agent 判定 + 用户偏好"先查 import 图与既有防护链"）

**第一梯队 · 堵资金风险（必须先做，顺序不可换）**：
1. **P1-6 skip_gate 加开关**（`execution.emergency_direct_exec: false`）—— 否则修 P0-5 反而放大 P1-6
2. **P0-5 黑名单 sell 豁免** —— 修完 P1-6 后再做
3. **P0-1 应急扫描补条件①** —— 根缺陷，已接线双 cron

**第二梯队 · 修现役生产事故**：
4. **launchd catchup 复活**（漏报-1）—— 中文路径编码 + TCC 授权
5. **P0-3 catchup 加 09:14 分支**（或专用 launchd 任务）—— catchup 复活后再做
6. **P0-2 confirm 豁免跨日 + run_date 改交易日历**

**第三梯队 · 数据正确性**：
7. **P0-6 腾讯字段全面修正**（f[19]/f[20] ask1、f[44]×1e8 float_mv、f[47]/f[48] limit_up/down）+ fixture 改真实报文
8. **P0-7 换 akshare 接口**（`stock_market_activity_legu`）
9. **P0-8 breadth 挪 postclose** + 区分"池为空"与"失败"
10. **P1-3 四处补 profile 过滤**
11. **P1-4 修 IVOL 对齐 + P1-5 backfill 强制 degraded=False** → 重跑 v2 backfill

**第四梯队 · 卫生**：
12. **P0-4 DB 隔离**（RiskContext 加 conn 字段或 AGSICKLE_DB 全覆盖）+ 清理生产库 108+ 条测试残留
13. **P1-1 关键词嵌套修复**（命中长词后屏蔽子串，或每标题每极性计 1）
14. **P1-2 stuck 计数去重 + 只在确认未跌停时清零**

**第五梯队 · UI/控制台**：
15. **P0-9 后端补 `/api/breadth`、`/api/limit_halt/stuck`；前端加宽度卡片与应急单专区**
16. **P1-8 GateCard 显示委托价/股数/risk_notes**
17. **P1-9 emergency_scan 单强制二次确认**
18. **P1-11 文案 15→21 条**（3 处字符串，5 分钟）

### 一句话结论

**Sprint 3 不通过结论成立**。原报告 21 条指控命中率 90%+，但漏报了 1 条现役生产事故（catchup launchd 瘫痪）与 1 条应升级为 P0 的资金风险（P1-6 skip_gate 已生效）。修复顺序必须"先堵 P1-6 自动成交路径，再放 P0-5 sell 豁免"，否则修 P0-5 反而放大 P1-6。当前生产**零持仓**使 D1 缺陷全部休眠——这是唯一的"减害"事实，但建首仓破线即发难，**必须在 P6 模拟盘试运行前完成第一梯队三条修复**。

---

## 附录 · 复核方法论

**4 个独立子代理并行核实**（按用户偏好"多 agent 交叉核实"）：
- Agent A：D1 应急链（P0-1/P0-2/P0-3/P1-2/P1-10）—— 独立 python 反例复现 + 接线图核实
- Agent B：资金风险链（P0-4/P0-5/P1-1/P1-6）—— DB 快照对比 + 反例复现 + 全仓 grep 决策读方
- Agent C：数据正确性（P0-6/P0-7/P0-8/P1-3/P1-4/P1-5）—— 联网直连 qt.gtimg.cn 抓真实报文 + akshare docstring 核实 + sqlite 只读实证
- Agent D：UI/控制台（P0-9/P1-7/P1-8/P1-9/P1-11/P1-12/P2）—— 前后端 grep + 组件渲染字段清单核对

**主 agent 亲验**：
- 测试基线：`pytest --collect-only -q` → 267 tests collected ✅
- 生产库：sqlite 只读查询 risk_event/signal/breadth_daily ✅
- git 状态：39 files, +3900/-269 ✅

**复核原则**（按用户 MEMORY.md 偏好）：
1. **区分「理论缺陷」与「生产事故级」**：每条指控都重估严重性，查 import 图与既有防护链
2. **先查 import 图与既有防护链再下结论**：例如 P0-4 查"limit_halt_emergency 是否有决策读方"→ 无 → 降级为审计事故
3. **不臆断**：所有数字（108 条污染、108405 行 signal、267 用例）都实测核实；所有字段语义（腾讯 f[42]）都联网抓真实报文核实

**复核过程未修改任何项目文件**；Agent A 的实测脚本置于 `/tmp/repro_p01.py`、`/tmp/repro_p02.py`、`/tmp/repro_p12_p110.py`；Agent B 的实测用 `AGSICKLE_DB=/tmp/p05_repro.db` 隔离。

---

**复核报告完**
