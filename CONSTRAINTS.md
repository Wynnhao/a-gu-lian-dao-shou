# A股镰刀手 · 约束章程（CONSTRAINTS）

> - **版本**：v1.0（2026-09-17）
> - **性质**：项目级约束治理总入口；任何新增 sprint/改动须先核对此文件
> - **维护**：重大约束变更需走 §6 流程；Sprint 启动前必须 grep 一遍
> - **关联**：`技术方案.md` §1.3（运行时硬约束）/ `docs/结构性重构实施方案.md` §0（工程 ADR）/ `docs/策略库借鉴-cloddsbot.md`（借鉴 ADR）/ `memory/MEMORY.md`（历史踩坑）

---

## 0. 约束分层

| 层 | 性质 | 变更难度 | 文档位置 |
|---|------|---------|----------|
| L1 运行时 | A 股交易制度硬约束（T+1/涨跌停/交易时段） | 不可变 | `技术方案.md` §1.3 |
| L2 工程纪律 | 代码组织、Python 版本、测试纪律 | 重大变更需 ADR | `docs/结构性重构实施方案.md` §0 |
| L3 风控红线 | 永不自动成交、confirm 闸门、PENDING 兜底、kill 链 | 不可绕 | `技术方案.md` §4 + §5 + 本文件 §3 |
| L4 策略约束 | "明确不做"的策略项（防反复提案） | 重大变更需用户拍板 | 本文件 §4 |

---

## 1. L1 运行时约束（不可变）

- ❌ **T+1**：当日买入的不可卖
- ❌ **不接实盘资金**：第一版为 paper 模拟盘
- ❌ **不交易**：ST 股、上市 < 60 日次新股、北交所、N 开头新股
- ❌ **不参与集合竞价**：9:15-9:25 不下单，9:30 后决策
- ❌ **涨跌停约束**：到板即拒（涨停不买、跌停不卖）

详见 `技术方案.md` §1.3。

---

## 2. L2 工程纪律（重大变更需 ADR）

浓缩自 `docs/结构性重构实施方案.md` §0：

| # | 约束 | 来源 |
|---|------|------|
| C-ENG-1 | **Python 3.9 语法**：禁 `X \| Y` 联合类型等 3.10+ 特性；可用 `from __future__ import annotations` | 结构性重构 ADR #9 |
| C-ENG-2 | **stdlib-only**：`common/` 下文件不引第三方包；配置校验用 dataclass 不引 pydantic | ADR #1 |
| C-ENG-3 | **配置可变快照**：测试依赖 `CFG` dict 可变（`set_gate()` 原地 mutate），**不做深冻结** | ADR #2 |
| C-ENG-4 | **返回值形状不变**：repo 函数返回被替换处现状形状（tuple/标量），不引 dataclass 包装 | ADR #3 |
| C-ENG-5 | **每个 Phase 独立 feature branch + squash merge**，测试不绿不进下一阶段 | ADR #10 |
| C-ENG-6 | **AGSICKLE_* env 语义不可改**：4 个运行时开关（DISABLE_LIVE_QUOTES/SLIPPAGE/NOTIFY/ALLOW_UI）保持"调用时读 env"语义 | ADR #7 |
| C-ENG-7 | **测试隔离**：子进程测试必须设 `AGSICKLE_DB` 隔离；postclose `date.today()` 不可注入，分支只能真实今天测；catchup flock 测试避免并发 | 结构性重构 §1 |

---

## 3. L3 风控红线（永不绕）

### 3.1 永不自动成交（最高级）

- ❌ **绝不让 LLM 自动下单**：runner 总原则；所有 `decision → trade` 必经人工 confirm
- ❌ **绝不引入 `skip_gate=True` 直写成交**：默认走 B 路径（confirm 闸门），即使规则 21 触发也只产 pending decision
- ❌ **绝不绕过 `risk_event` 落库**：任何风控动作（kill/软减仓/封单应急/止盈）必须留痕

> 唯一历史例外 `decision['skip_gate']` 仅在规则 21 触发 + 显式启用时生效，**默认禁用**（参见 `docs/策略库.md` Sprint 1+2 §9 落地建议"已落地新规则表"）。

### 3.2 confirm 闸门契约

- runner propose → 落 pending 文件 → 等 `runner.py confirm --decision-id N` → confirm 时**重跑风控**
- 不允许"事后 confirm"（成交后才补 confirm）
- PENDING 闭环 24h 必须有结果（成交 / reject / 过期）；0 泄漏是硬验收

### 3.3 优先级链（强序）

```
强平（事件/到期）> 止损（规则 16 ATR）> 追踪止盈（规则 22）> 时间退出（规则 23）> 止盈（固定档）> 时间退出
                  > 软减仓（规则 25，15% 预警，不 kill）> 单票上限（规则 6 max_single_weight）
                  > kill（规则 5 8% 紧急熔断，停机 72h）
                  > kill_liquidation 补清算（T+1/卖出失败递延次日照常放行，豁免执行熔断）
                  > 执行熔断（C-ARC-2：当日 F1a∪F2 根决策 ≥3 → 暂停新 propose，当日自动解除）
                  > 确认人闸门（最高级裁判）
```

后续新增规则必须**显式声明在本链中的位置**（参见借鉴清单 #1 出场优先级显式化）。

### 3.4 PENDING 兜底链

- fetcher 数据缺失 / kill 退出（rc=2）→ 写 `pending` 状态
- catchup 步骤 4 盘后当日补全（15:10 后 PENDING → 重跑 postclose）
- HealthPanel 红警兼容（不静默吞错）
- `_restore_env` 必须清 AGSICKLE_* 变量（修复见 `docs/审计补丁批FixA-F落地`）

---

## 4. L4 策略约束（明确不做 / 暂缓 / 降级）

> **本节是借鉴清单 + 历史策略库条目里"明确不做"的集中归档**。未来 sprint 提案如涉及以下项，必须先读本节 + `docs/策略库借鉴-cloddsbot.md` §4 + `docs/策略库.md` 对应章节；推翻任何一条需用户拍板。

### 4.1 借鉴清单明确拒绝 / 暂缓项（CloddsBot 调研，2026-09-17）

| # | 策略项 | 状态 | 不做的根因 | 复活条件 |
|---|--------|------|-----------|----------|
| C-STR-1 | **ladder buy/sell 阶梯买卖** | ❌ **拒绝** | (1) T+1 制度冲突（当日买入的不可卖卡阶梯第二档）；(2) UI 复杂度 3x；(3) 5 票 core 池分档无意义 | 项目扩到 ≥10 只 core + 引入分钟级 paper 撮合 + 有人承担 ladder UI 重构 |
| C-STR-2 | **拉盘防御（pump defense）** | ⚠️ **暂缓** | A 股涨跌停约束下"急涨 X% 卖 Y%"信号发出日 ≠ 可执行日（涨停下买不进、跌停下卖不出） | "开板后 N 分钟内急涨 X%" 立项；或实现为不进 pending 的纯预警 |
| C-STR-3 | **动态凯利（强版，1/4 Kelly + 胜率赔率估计）** | ❌ **拒绝** | (1) 样本 < 6 个月时凯利输出是伪精确；(2) 与现有规则 5 8% kill 永远先到；(3) 审查报告 §8.3 已明示"不要绕过硬规则让 LLM 更灵活" | 样本 ≥6 个月独立观测 + 规则 5 阈值上调 ADR |
| C-STR-4 | **consensus_compare（一致预期 edge）** | ⚠️ **降级** | (1) akshare 无独立 consensus 接口；(2) A 股覆盖率低（≈30%）+ 披露口径不一；(3) 回测必前视、生产必缺数据 | akshare/聚宽出现独立接口且覆盖率 ≥70% |
| C-STR-5 | **龙虎榜席位评分 / 跟随** | ⚠️ **暂缓** | (1) 2026 监管整治"游资战法"，披露规则酝酿优化；(2) 项目原决策已 [暂缓] | 监管政策明朗 + 席位评分层离线跑通 |
| C-STR-6 | **北向资金日频择时** | ⚠️ **暂缓** | 2024-05-13 取消盘中实时披露；2024-08-19 停止每日净买入、持股改季度——**日频规则已无数据支撑**（参见 `docs/策略库.md` §4.3 [降级为 LLM 背景信息]） | 北向披露规则恢复日频 |

### 4.1b 借鉴清单明确拒绝 / 暂缓项（CloddsBot 架构层调研，2026-09-19）

| # | 架构项 | 状态 | 不做的根因 | 复活条件 |
|---|--------|------|-----------|----------|
| C-ARC-6 | **SignalBus 事件总线** | ❌ **拒绝** | 日频 4 节点短命进程 + SQLite/文件交接是有意的简单架构；总线=常驻进程+新故障面，与 PENDING 兜底/kill 链可靠性模型冲突（CloddsBot 需要总线是因连续 Tick 流 + 21 渠道并发） | 盘中连续决策（分钟级策略）上线且出现 ≥3 个生产者-消费者对 |
| C-ARC-7 | **动态 Kelly 反馈回路（架构层复述）** | ❌ **不复议** | 同 C-STR-3（样本不足 + 规则 5 永远先到 + §8.3"不要绕过硬规则让 LLM 更灵活"） | 同 C-STR-3 |
| C-ARC-8 | **21 渠道接入 / TWAP / DCA / MEV 防护** | ❌ **拒绝** | 加密特化；TWAP 对 paper 撮合是错误建模（A 股散户无算法拆单通道）；渠道接入现有 webapp+终端+看门狗足够 | （无——实质违反 L1"不接实盘"边界即永不） |
| C-ARC-9 | **盘中连续盯市（分钟级加密监控）** | ⚠️ **暂缓** | 依赖 minute_snapshot 先录 ≥2 周数据，量化「11:35/14:50 双窗之间漏触线」再定加密频率（防拍脑袋加密度） | 录制数据显示窗外触线 ≥1 次/周且造成过实际损失 |

### 4.2 历史策略库暂缓项（来自 `docs/策略库.md` Sprint 2 清理）

| # | 策略项 | 状态 | 复活条件 |
|---|--------|------|----------|
| C-STR-7 | **小市值轮动**（§2.2） | ⚠️ 暂缓 | P1-1 市场宽度熔断就位 + 2026 拥挤度出清的媒体报道确认 |
| C-STR-8 | **龙虎榜席位跟随**（§4.2） | ⚠️ 暂缓 | 同 C-STR-5 |
| C-STR-9 | **北向资金择时**（§4.3） | ⚠️ 降级为 LLM 背景 | 同 C-STR-6 |

### 4.3 跨市场借鉴总约束

- **不直接搬加密/二元市场策略**：CloddsBot（Polymarket/Solana/Kalshi）与 A 股 T+1 限价撮合、涨跌停、散户主导结构性异构。任何借鉴必须经"异构分析 → 工程可行性 → 有机结合"三方调研（参见 `docs/策略库借鉴-cloddsbot.md` §0）
- **不引入与现有架构耦合的"自适应层"**：审视者三方调研点出，v2 profile 拥挤降权 + regime vol_target + 借鉴 #7 三个自适应信号同时存在会形成"反馈振荡器"。借鉴 #7 波动率自适应**仅在 regime=normal 启用**，shock/off 档禁用（见 ADR）

---

## 5. 测试纪律（验收前必查）

| # | 项 |
|---|----|
| C-TEST-1 | 新增规则/通道在 `tests/` 必有对应测试用例 |
| C-TEST-2 | `risk/engine.py:897` demo 区对每条新规则至少有 3 个场景 case |
| C-TEST-3 | 子进程测试必须设 `AGSICKLE_DB` 隔离真实库；postclose L182 `date.today()` 分支只能在真实今天测 |
| C-TEST-4 | 测试禁止污染生产 `market.db`；已有保护见 `_restore_env`（清理 AGSICKLE_*） |
| C-TEST-5 | `signals/backtest.py` 必须用可成交口径 + 三重成本 + 中证 800 宇宙（防前视） |
| C-TEST-6 | 生产代码 broker 成交调用点只允许 `runner._execute`/`_do_kill` 两处（`tests/test_guard_invariant.py` AST 守护）；新增调用点必须显式修订白名单并通过 review，**不得绕过/删除守护测试** |

---

## 6. 约束变更流程

1. **提案**：在某 sprint plan / issue / 对话中提出"想改约束 X"
2. **检索**：先 grep 本文件 + `docs/策略库借鉴-cloddsbot.md` §4 + `docs/策略库.md` 对应章节 + `memory/MEMORY.md` 相关踩坑
3. **ADR**：写一份微型 ADR（结论 + 理由 + 反方论点 + 替代方案），更新本文件对应条目
4. **用户拍板**：L1/L3 不可改；L2/L4 重大变更需用户拍板
5. **同步**：更新关联文档（`技术方案.md` / `docs/结构性重构实施方案.md` §0 / `memory/MEMORY.md`）
6. **不静默删除**：被推翻的约束条目保留在 ADR 附录里（参见 `docs/结构性重构实施方案.md` §0 风格）

---

## 附录 A · 借鉴清单 ADR 索引

| ADR | 决策 | 文件位置 |
|-----|------|----------|
| 借鉴 #1 出场优先级 | 采纳 | `docs/策略库借鉴-cloddsbot.md` §1 ADR#1 |
| 借鉴 #2 拉盘防御 | 暂缓 | 同上 ADR#2 + 本文件 C-STR-2 |
| 借鉴 #3 ladder | 拒绝 | 同上 ADR#3 + 本文件 C-STR-1 |
| 借鉴 #4 业绩预告（降级） | 降级采纳 | 同上 ADR#4 + 本文件 C-STR-4 |
| 借鉴 #5 封单/开板 | 采纳 | 同上 ADR#5 |
| 借鉴 #6 聪明钱（降级） | 降级采纳 | 同上 ADR#6 + 本文件 C-STR-5/6 |
| 借鉴 #7 vol_regime（受控） | 采纳 | 同上 ADR#7 + 本文件 §4.3 |
| 借鉴 #8 凯利（强版） | 拒绝 | 同上 ADR#8 + 本文件 C-STR-3 |
| 借鉴 #8b 软减仓 | 采纳（弱化） | 同上 ADR#8b |
| 借鉴 #9 三态语义 | 采纳 | 同上 ADR#9 |
| C-ARC-1 执行级有限重挂（F1a 保守版） | 采纳（审核 pass-with-fix） | `docs/架构借鉴-cloddsbot-2026-09-19.md` ADR-A1 + `docs/C-ARC架构加固施工方案-2026-09-19.md` §2.T3 + `docs/ADR-0-执行失败taxonomy-F1a-F1b-F2.md` |
| C-ARC-2 执行失败熔断 | 采纳 | 同上 §2.T4；优先级链见本文件 §3.3 |
| C-ARC-3 分钟快照录制（受控） | 采纳（"不引新表"例外仅限 `minute_snapshot`） | 同上 §2.T5-T7 + `技术方案.md` §9 |
| C-ARC-4 拒绝/告警计数补全 | 采纳 | 同上 §2.T2 |
| C-ARC-5 门卫守护测试 | 采纳 | 同上 §2.T1 + 本文件 §5 C-TEST-6 |
| C-ARC-6/7/8 拒绝项 | 拒绝 | 本文件 §4.1b |

---

## 附录 B · 维护记录

- v1.0（2026-09-17）：初版；首屏纳入借鉴 CloddsBot 9 项调研结论 + 结构性重构 §0 ADR 浓缩 + 历史策略库暂缓项同步
- v1.1（2026-09-19）：C-ARC 架构层借鉴回填——§3.3 优先级链插入 kill_liquidation 补清算与执行熔断（C-ARC-2）、§4.1b 新增 C-ARC-6/7/8/9、§5 增 C-TEST-6、附录 A 增 C-ARC ADR 索引