# 安全策略

## 重要声明：本项目是 paper-only 模拟盘

**A股镰刀手（AGSICKLE）是一个仅在本地运行的模拟交易系统，不连接任何真实券商账户、不执行任何真实交易。** 任何配置为 `execution.mode: paper` 的运行都是模拟，所有资金变动均为纸面记账。

**严禁**将本项目用于真实交易。我们不提供、不支持、不建议任何形式的实盘对接。

## 漏洞披露

如果你在本项目中发现了安全漏洞，请通过以下方式私密联系，**不要**在公开 Issue 中披露：

- **首选**：GitHub 私有漏洞报告（Security Advisories）功能
- **备选**：项目维护者邮箱（如仓库 `MAINTAINERS.md` 中所示）

请在报告中包含：

1. 受影响的版本（commit hash 或 tag）
2. 漏洞类型与影响面
3. 复现步骤与最小 PoC
4. 你的修复建议（如有）

我们会在收到报告后 7 个工作日内确认，并在修复后公开致谢（除非你希望匿名）。

## 已知的非漏洞"问题"

### 写接口暴露在 localhost

Web 控制台（`webapp/server.py`）默认绑定 `127.0.0.1:8317`，不暴露到公网。如果你把它部署到公网：

- 必须开启 Host 校验（已默认开启，参见 `webapp/server.py`）
- 必须启用 HTTPS（建议反向代理 Nginx/Caddy）
- 必须开启鉴权（当前无内置鉴权，依赖反向代理）

### 模拟盘日志可能包含个人策略指纹

`logs/` 目录下的决策日志、回测结果、日报是模拟盘的产物，不会泄露真实资金。但其中包含的具体股票代码、阈值参数、LLM 决策理由可能反映**作者个人的投资偏好**。如果你的日志里包含此类信息，建议：

- 不要把 `logs/` 提交到任何公开仓库
- 分享截图前脱敏股票代码

### 数据源 ToS

本项目使用的行情/资讯数据源（东方财富、新浪、雪球、腾讯财经、乐咕乐股等）均为公开 API，但各家**都不允许大批量爬取用于商业用途**。请遵守：

- 配置里的 `source_cooldown_min: 30`（采集间隔）已经做了节流
- `source_max_fail: 5`（熔断阈值）失败后会自动降级到下一个数据源
- 不要降低节流参数、不要并发爬取、不要用作商业服务

## 版本与支持

仅最新主分支（`main`）会收到安全更新。历史版本不提供补丁。

## 致谢

负责任披露漏洞的研究者将在修复公告中致谢（除非你希望匿名）。
